# 环���变量���理界面设计

## 1. 概述

### 1.1 当前状态分析

**已���现功���**：
- ✅ ���境数据存储（Zustand store）
- ✅ 环境选���器（EnvSelector）- 切换/添加/删���环境
- ✅ ���量查���器（VariableInspector）- 只读���看所���变量
- ✅ 变量���析和���换（VariableResolver）
- ✅ 持久���到磁���（IPC）

**���失功���**：
- ❌ ���境变���编辑���面 - **核心���失**
- ❌ 全���变量编辑界面
- ❌ ���境类���设置���dev/staging/production）
- ❌ 批量导���/导出变量

### 1.2 设���目标

1. **易���性**���提供直���的变���编辑体验���类似 Params/Headers 的 KeyValueEditor
2. **���整性**：支持环���变量���全局变���的完整 CRUD 操���
3. **���致性**：UI ���格与现���组件���持一致
4. **实时性**：编辑���立即���效，自���持久���

---

## 2. 界���设计

### 2.1 入���设计

#### 方案���：Settings 页面（推���）
在 Settings 中添��� "Environments" 分类，与���有的 General、Proxy 等���列。

**优点**：
- ���合用���心智���型（���境配���属于���置）
- 有���够空���展示复���的编辑界���
- 可以同时���理多个���境

**缺点**：
- 需要���开 Settings 才能编辑，���骤较���

#### ���案二���EnvSelector 扩展
点击 EnvSelector 下拉菜单���的环���名称右侧的���辑按���，打���编辑���窗。

**优点**：
- 快速���问，就近���辑
- 适���快速���改单���变量

**缺点**：
- 弹窗���间有���
- 不适合���理多���环境

#### 方案三：���立页面
在侧���栏添加 "Environments" 入口���打开���立的环境���理页���。

**���点**：
- 专���的编辑体���
- 可���展示更多���息和���能

**缺点**：
- ���加侧边栏���杂度
- 与现有导航结���不一致

**最���选择**：**方案一（Settings 页面���+ 方案���（快速编���）组���**
- 主要���辑入���在 Settings
- EnvSelector 提供快���编辑���钮

---

### 2.2 Settings 页面 - Environments 分���

#### 2.2.1 布局���构

```
┌───���──���──────���──���────���──���──���────────���──���──────���──���────���──���──���┐
│ Settings                                              [×]   │
├───���──���───���──���┬────���──���────���───���──���────���───���─────���──���──���──���──┤
│ General      │  Environments                                │
│ Proxy        │                                              │
│ Mock Server  │  ┌──���──���───���──���────���──���──���──���────���───���──���─┐ │
│ Cookies      │  ��� [Dev ●] Development            [Edit]  │ │
��� Fonts        │  │ [Stg ●] Staging                [Edit]  │ │
│ Data         │  │ [Prd ●] Production             [Edit]  │ │
│ About        ���  │                                         │ │
│► Environments���  │ [+ Add Environment]                    │ │
│              ���  └─���────���───���──���───���──���──────���──���────���───���┘ │
│              │                                              │
│              │  Global Variables                            │
│              │  ┌─���──���────���──���──���──���────���────���──���──────���─┐ │
│              │  │ ☑ timestamp    {{$timestamp}}          │ │
���              │  │ ��� guid         {{$guid}}               │ ���
│              ���  │ ☑ randomInt    {{$randomInt}}          │ │
���              │  │                                         │ │
│              ���  │ [+ Add Global Variable]                │ │
│              │  ���──────���──���────���────���──���───���──���────���──���──��� │
���───���──���────���──┴���──���────���──���──────���─────���────���─────���──���──────���┘
```

#### 2.2.2 环境列表���域

**环境���片**：
- 环���类型标识���彩色���点）
- ���境名称
- 变量���量提示（��� "5 variables"）
- 编辑按钮
- ���除按钮（hover 显示���
- 激活���态指���（当前激活的���境高���）

**添加���境按���**：
- 点���后创建新���境，���认名��� "New Environment"
- 自动���开编���弹窗

#### 2.2.3 全局���量区域

**内置���态变量**（只读）：
- `{{$timestamp}}` - 当前���间戳
- `{{$guid}}` - 随机 GUID
- `{{$randomInt}}` - ���机整���
- `{{$randomString}}` - 随机���符串

**自定义全���变量**：
- 使用 KeyValueEditor 风���的编���器
- 支持启用/���用
- 支持���加/删除

---

### 2.3 环境编辑���窗

#### 2.3.1 弹���布局

```
┌──���──���──���──���───���───���──���────���──���──���────���──���────���──���──���──���─┐
│ Edit Environment: Development                     [×]   │
���──���──���─────���──���─────���──���────���────���───���──���──���──────���──���──���┤
│                                                         │
���  Environment Name                                       │
│  ┌──���──���──���────���───���──���─────���──���─────���──���────���──���────��� │
│  │ Development                                       │ │
│  ���───���──���────���──���─────���──���──���──���──���──���───���──���──���──���──┘ │
│                                                         │
│  Environment Type                                       │
│  ┌──���───���──���────���──���──���──���────���────���──���────���──���──���──���┐ ���
│  ��� [●] Development  [ ] Staging  [ ] Production      │ │
│  └──���──���──────���──���────���──���────���───���──���────���──���──���────┘ ���
│                                                         │
│  Variables                                              ���
│  ┌───���──���──���──���──���───────���──���──────���──���───���──���────���──��� │
│  │ ���  Key              Value              [×]        │ │
│  ���────���──���──���──���────���──���──────���──���─────���──���──────���──���┤ ���
│  ��� ☑  base_url         http://localhost:3000  [×]   │ │
│  │ ☑  api_key          dev-key-123            [×]   │ │
���  │ ���  debug_mode       true                   [×]   │ ���
│  │                                                   │ ���
│  ��� [+ Add Variable]                                 │ │
���  └──���──���────���───���──���────���──���──���──────���──���──���──���────���─┘ │
│                                                         │
���                          [Cancel]  [Save]              │
└───���───���──���───���──────���──���──────���──���─────���──���────���───���──���─┘
```

#### 2.3.2 字段���明

**Environment Name**：
- 文本输入���
- 必填���不能为���
- 实���验证

**Environment Type**：
- 单���按钮���
- 选项：Development（���色）���Staging（黄���）、Production（红色���
- 影响 EnvSelector ���的颜���显示

**Variables**：
- 使用 KeyValueEditor 组件
- 支���启用/���用（checkbox）
- 支持���加/删除���
- 自动添���空行
- ���持拖���排序���可选）

**���作按���**：
- Cancel：关闭���窗，���保存
- Save：保存���持久���到磁���

---

### 2.4 EnvSelector 快速编���

#### 2.4.1 ���拉菜单扩���

```
���──────���───���───���──���──────���──���──���─┐
│ ● Development          ✓  [⚙] │  ← ���活的���境，显���编辑���标
��� ● Staging                 [���] │
��� ● Production              [⚙] ���
├���────���──���──���───���──���──���──────���───┤
│ + Add Environment              │
└─���───���──���─────���──���──���───���──���────���
```

**交互���辑**：
- 点击���境名称���切换���活环境
- 点击 [⚙] 图标���打开编辑弹���
- Hover 时显���变量数量���示

---

## 3. ���据流���计

### 3.1 ���态管���

**EnvironmentStore** (已存在，���扩展)：

```typescript
interface EnvironmentStore {
  // 现有���态
  environments: Environment[];
  activeEnvironmentId: string | null;
  globals: Variable[];
  isLoaded: boolean;

  // 现有方���
  setActiveEnvironment: (id: string | null) => void;
  addEnvironment: (env: Environment) => void;
  updateEnvironment: (id: string, updates: Partial<Environment>) => void;
  deleteEnvironment: (id: string) => void;
  loadFromDisk: () => Promise<void>;
  persistEnvironment: (id: string) => void;

  // 需要新增的���法
  addVariable: (envId: string, variable: Variable) => void;
  updateVariable: (envId: string, key: string, updates: Partial<Variable>) => void;
  deleteVariable: (envId: string, key: string) => void;
  addGlobalVariable: (variable: Variable) => void;
  updateGlobalVariable: (key: string, updates: Partial<Variable>) => void;
  deleteGlobalVariable: (key: string) => void;
}
```

### 3.2 ���久化���略

**自���保存**：
- 编辑���境名称：���时保存
- 添加/删除/修改���量：���时保���
- 使用 debounce（300ms）避���频繁写���

**手动保存**：
- 编辑弹窗中的 Save 按钮：���量保���所有修改

**推���方案**：手动保���（与现��� RequestPanel ���持一���）

---

## 4. 组件���计

### 4.1 新增���件

#### 4.1.1 EnvironmentEditor

**路径**：`src/components/environment/EnvironmentEditor.tsx`

**功���**：
- 编���单个环境的所有信���
- 可作为弹���或内���组件使用

**Props**：
```typescript
interface EnvironmentEditorProps {
  environmentId: string;
  onSave?: () => void;
  onCancel?: () => void;
}
```

#### 4.1.2 EnvironmentList

**路径**：`src/components/environment/EnvironmentList.tsx`

**功���**：
- 显���所有���境的列表
- 支持���加/删除/编辑

**Props**：
```typescript
interface EnvironmentListProps {
  onEditEnvironment: (id: string) => void;
}
```

#### 4.1.3 GlobalVariablesEditor

**路径**：`src/components/environment/GlobalVariablesEditor.tsx`

**功能**：
- 编辑全局���量
- 显示内置动���变量（只���）

**Props**：
```typescript
interface GlobalVariablesEditorProps {
  // 无需 props���直接从 store ���取
}
```

### 4.2 复用���件

- **KeyValueEditor**：用于变���列表���辑
- **Dialog**（Radix UI）：���于编辑弹���
- **SettingsToggle**：用���启用/禁用变���

---

## 5. 交互流程

### 5.1 编辑���境变量

```
用户操���                          系统���应
──���──���────���──���────���──���──���──���────���───���──���────���──���──���────
1. 打��� Settings                → 显示 Environments 分类
2. 点击环���的 [Edit] 按钮       → 打开���辑弹���
3. 修改环境���称                 → 实时���证
4. 添���/修改/删除���量           → 更���本地状态
5. 点��� [Save]                  → 持久���到磁���
                                ��� 关闭弹���
                                → ���示成���提示
```

### 5.2 ���速编���

```
用户���作                          系���响应
──���───���──���──���──���──���──���──���────���──���──���──���────���──���──���─────
1. 点��� EnvSelector             → 显示���拉菜单
2. 点���环境的 [⚙] 图标          → ���开编辑弹���
3. 修���变量                     → ���新本���状态
4. 点��� [Save]                  → 持久化到���盘
                                → 关闭弹窗
```

### 5.3 添加���环境

```
用户操作                          系统���应
───���───���──���────���──���──────���──���────���──���──���────���────���──���──
1. 点击 [+ Add Environment]     → ���建新环���（默���名称）
                                ��� 打开编���弹窗
2. 输入���境名���                 → 实时验���
3. 选���环境���型                 → 更新���色标识
4. 添加变量                     → 更���变量列表
5. 点击 [Save]                  → 持久���到磁���
                                → ���闭弹���
                                → ���境列���中显示新环���
```

---

## 6. 视���规范

### 6.1 颜色系统

**环境类���颜色**：
- Development：`text-accent-success` / `bg-accent-success/10`
- Staging：`text-accent-warning` / `bg-accent-warning/10`
- Production：`text-accent-danger` / `bg-accent-danger/10`

**状���颜色**：
- 激活���境：`bg-brand-muted` / `text-brand`
- 禁用变量���`opacity-40` / `line-through`

### 6.2 尺寸���范

**弹窗尺���**：
- 宽度：`560px`
- 高度：`auto`（���大 `80vh`）

**输入框���度**���
- 环境名���：`h-[32px]`
- 变量行���`h-[32px]`

**间距**：
- 字段���距：`gap-3`（12px）
- 按���间距：`gap-2`（8px）

### 6.3 字体���范

- 标题：`text-sm font-semibold`
- 标���：`text-[11px] font-semibold text-fg-secondary`
- 输入���：`text-[12px] font-mono`
- 按钮：`text-[12px] font-medium`

---

## 7. 技术实现要���

### 7.1 表单验���

**���境名���**：
- ���能为空
- 不能���现有环境重名���除自���）
- ���度限制：1-50 字符

**变量 Key**：
- 不能���空
- 同一���境内���能重���
- 建议���式：`snake_case` 或 `camelCase`

### 7.2 性能优化

**大量���量场���**：
- 使用���拟滚���（如果变量���过 100 个）
- Debounce 输���事件（300ms）

**持���化优化**：
- 批量���存，避免���繁 IPC 调用
- 使用 Immer 减少���必要���重新渲染

### 7.3 错误���理

**持久化���败**：
- 显示 Toast 错误提���
- 保留���户输入，���许重试

**加���失败**：
- 显���默认环���
- 提示用户检���文件���限

---

## 8. 实���优先���

### Phase 1：核心功能（���须）
1. ✅ Settings 页���添加 Environments 分类
2. ✅ EnvironmentList 组件
3. ✅ EnvironmentEditor 弹窗
4. ✅ 环境���量 CRUD 操作
5. ✅ 持久化���磁盘

### Phase 2：���强功能（���要）
1. ⭕ GlobalVariablesEditor 组���
2. ��� EnvSelector 快速���辑按钮
3. ⭕ 环���类型选择
4. ⭕ ���单验���

### Phase 3���高级���能（���选）
1. ⚪ 批���导入/导出（JSON/CSV）
2. ⚪ 变量拖拽���序
3. ⚪ 变量���索/���滤
4. ⚪ 变量使用���计（���示哪些请求使���了该���量）

---

## 9. 测���计划

### 9.1 单元���试

- EnvironmentStore 的所���方法
- 表���验证逻辑
- 变量解析���替换

### 9.2 集成���试

- 创���环境 → 添加变量 ��� 保��� → 重新���载
- 切换���境 → 变量���效
- 删除环境 ��� 清���数据

### 9.3 E2E 测试

- 完整的���户操作流���
- 跨组件���互（EnvSelector + VariableInspector + RequestPanel）

---

## 10. 后续���化方���

1. **���境继承**：子环境可���继承���环境���变量
2. **环境���组**���按项目或团队���组管���环境
3. **���量加密**：敏感���量（��� API Key���加密存储
4. **变量历史**：记���变量���改历���，支���回滚
5. **团队协作**：导出/���入环境配���，团���共享

---

## 11. ���考资���

- Postman Environments: https://learning.postman.com/docs/sending-requests/managing-environments/
- Insomnia Environments: https://docs.insomnia.rest/insomnia/environment-variables
- 现有���码：
  - `apps/desktop/src/stores/environment-store.ts`
  - `apps/desktop/src/components/url-bar/EnvSelector.tsx`
  - `apps/desktop/src/components/drawers/VariableInspector.tsx`
