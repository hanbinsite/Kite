# 集���级配置完整���计

> 创建���间：2026-05-06
> ���态：设计���

## 1. 概述

实现三级���置继���体系：**Collection → Folder → Request**，���持 Headers、Auth、Variables、Scripts 的���承和���盖。

## 2. 数据���构设计

### 2.1 TypeScript 类型定义

```typescript
// packages/types/src/index.ts

/** 集合配置 */
export interface CollectionConfig {
  headers?: Header[];
  auth?: AuthConfig;
  variables?: Variable[];
  scripts?: ScriptConfig;
}

/** 文件���配置 */
export interface FolderConfig {
  headers?: Header[];
  auth?: AuthConfig;
  variables?: Variable[];
  scripts?: ScriptConfig;
}

/** 脚本���置 */
export interface ScriptConfig {
  preRequest?: string;      // 前置���本
  postResponse?: string;    // 后置���本（���含测试���
}

/** 完整的���合数���结构 */
export interface Collection {
  id: string;
  name: string;
  description?: string;
  version?: string;
  
  // 集合���配置
  config?: CollectionConfig;
  
  // 集合内���
  items: CollectionTreeNode[];
  
  // ���数据
  createdAt: string;
  updatedAt: string;
}

/** ���合树节点 */
export type CollectionTreeNode = CollectionRequest | CollectionFolder;

/** 集合中的请��� */
export interface CollectionRequest {
  type: "request";
  id: string;
  name: string;
  method: HttpMethod;
  url: string;
  
  // 请求���配置���可覆���上级���
  headers?: Header[];
  params?: QueryParam[];
  auth?: AuthConfig;
  body?: BodyConfig;
  scripts?: ScriptConfig;
  settings?: RequestSettings;
}

/** ���合中的文���夹 */
export interface CollectionFolder {
  type: "folder";
  id: string;
  name: string;
  description?: string;
  
  // 文���夹级配置���可覆盖集合���）
  config?: FolderConfig;
  
  // ���项
  items: CollectionTreeNode[];
}
```

### 2.2 Rust 数据结���

```rust
// apps/desktop/src-tauri/src/commands/collection.rs

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CollectionFile {
    pub id: String,
    pub name: String,
    pub description: Option<String>,
    pub version: Option<String>,
    
    // 集合级���置
    pub config: Option<CollectionConfig>,
    
    pub items: Vec<CollectionItem>,
    pub created_at: String,
    pub updated_at: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CollectionConfig {
    pub headers: Option<Vec<SavedHeader>>,
    pub auth: Option<AuthConfig>,
    pub variables: Option<Vec<CollectionVariable>>,
    pub scripts: Option<ScriptConfig>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FolderConfig {
    pub headers: Option<Vec<SavedHeader>>,
    pub auth: Option<AuthConfig>,
    pub variables: Option<Vec<CollectionVariable>>,
    pub scripts: Option<ScriptConfig>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ScriptConfig {
    pub pre_request: Option<String>,
    pub post_response: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(tag = "type")]
pub enum CollectionItem {
    #[serde(rename = "request")]
    Request(SavedRequest),
    
    #[serde(rename = "folder")]
    Folder(CollectionFolder),
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CollectionFolder {
    pub id: String,
    pub name: String,
    pub description: Option<String>,
    pub config: Option<FolderConfig>,
    pub items: Vec<CollectionItem>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SavedRequest {
    pub id: String,
    pub name: String,
    pub method: String,
    pub url: String,
    pub headers: Vec<SavedHeader>,
    pub params: Vec<SavedParam>,
    pub body: Option<SavedBody>,
    pub auth: Option<AuthConfig>,
    pub scripts: Option<ScriptConfig>,
    pub settings: SavedSettings,
}
```

## 3. 配���继承规则

### 3.1 Headers 继���

**规则**：合并所���层级��� Headers，下���覆盖同��� key

```typescript
function mergeHeaders(
  collectionHeaders: Header[] = [],
  folderHeaders: Header[] = [],
  requestHeaders: Header[] = []
): Header[] {
  const merged = new Map<string, Header>();
  
  // 1. 集合��� Headers
  collectionHeaders.forEach(h => {
    if (h.enabled) merged.set(h.key.toLowerCase(), h);
  });
  
  // 2. 文���夹级 Headers（覆盖同���）
  folderHeaders.forEach(h => {
    if (h.enabled) merged.set(h.key.toLowerCase(), h);
  });
  
  // 3. 请求��� Headers���最高优���级）
  requestHeaders.forEach(h => {
    if (h.enabled) merged.set(h.key.toLowerCase(), h);
  });
  
  return Array.from(merged.values());
}
```

**示例**：
```
Collection: [{ key: "X-API-Key", value: "{{api_key}}" }]
Folder:     [{ key: "Content-Type", value: "application/json" }]
Request:    [{ key: "X-API-Key", value: "override-key" }]

最终���果:
[
  { key: "X-API-Key", value: "override-key" },      // 请求级���盖
  { key: "Content-Type", value: "application/json" } // 文���夹级
]
```

### 3.2 Auth 继���

**规���**：���求级 > 文件���级 > 集���级，���合并

```typescript
function resolveAuth(
  collectionAuth?: AuthConfig,
  folderAuth?: AuthConfig,
  requestAuth?: AuthConfig
): AuthConfig | undefined {
  // 优先级：Request > Folder > Collection
  if (requestAuth && requestAuth.type !== "none") {
    return requestAuth;
  }
  if (folderAuth && folderAuth.type !== "none") {
    return folderAuth;
  }
  if (collectionAuth && collectionAuth.type !== "none") {
    return collectionAuth;
  }
  return undefined;
}
```

### 3.3 Variables 继���

**作用���优先���**（���高到低）���
```
1. Local Variables      (pm.variables.set 设置的���时变���)
2. Data Variables       (CSV/JSON 数���文件)
3. Environment Variables (当前激活���环境)
4. Collection Variables  (集合���量)
5. Global Variables      (全局变���)
```

**解析���辑**���
```typescript
function resolveVariable(key: string, context: ExecutionContext): string {
  // 1. Local
  if (context.localVariables.has(key)) {
    return context.localVariables.get(key);
  }
  
  // 2. Data
  if (context.dataVariables.has(key)) {
    return context.dataVariables.get(key);
  }
  
  // 3. Environment
  const env = context.activeEnvironment;
  if (env) {
    const envVar = env.variables.find(v => v.key === key && v.enabled);
    if (envVar) return envVar.value;
  }
  
  // 4. Collection
  const collection = context.currentCollection;
  if (collection?.config?.variables) {
    const colVar = collection.config.variables.find(v => v.key === key && v.enabled);
    if (colVar) return colVar.value;
  }
  
  // 5. Global
  if (context.globalVariables.has(key)) {
    return context.globalVariables.get(key);
  }
  
  return `{{${key}}}`; // ���找到，保持原���
}
```

### 3.4 Scripts 执���顺序

**Pre-Request Scripts**（���求前）：
```
1. Collection Pre-Request Script
2. Folder Pre-Request Script (if exists)
3. Request Pre-Request Script
   ���
   如果任���脚本���行失��� → ���止请���，显���错误
   ↓
4. 发送 HTTP 请���
```

**Post-Response Scripts**（响���后）：
```
   接收 HTTP 响���
   ↓
1. Request Post-Response Script (Tests)
2. Folder Post-Response Script (if exists)
3. Collection Post-Response Script
   ↓
   如果任���脚本���行失��� → ���记测试失���，显示错误
```

**错���处理**：
```typescript
interface ScriptExecutionResult {
  success: boolean;
  error?: {
    message: string;
    stack?: string;
    line?: number;
  };
  tests?: TestResult[];
}

// Pre-Request 失败 ��� 中���请求
if (!preRequestResult.success) {
  throw new Error(`Pre-request script failed: ${preRequestResult.error.message}`);
}

// Post-Response 失败 → 记录但���中止
if (!postResponseResult.success) {
  console.error(`Post-response script failed: ${postResponseResult.error.message}`);
  // 仍然���示响���，但标记测试���败
}
```

## 4. UI 设计

### 4.1 集合设���入口

**方式 1：双���集合���称**
```
Sidebar
└─��� Collections
    └── My API Collection  ��� 双���打开���置 Tab
```

**方式 2：右键菜���**
```
右键���合 → Collection Settings
```

**方式 3：集���标题栏���钮**
```
[Collection Name]  [⚙️ Settings]
```

### 4.2 集合设置 Tab 页面

打开���个新的 Tab（类���请求 Tab）���包含以���子页���：

```
Tab: "My API Collection Settings"

├─��� General
│   ├── Name: [My API Collection]
���   ├─��� Description: [Textarea]
���   └���─ Version: [1.0.0]
���
├─��� Auth
│   ├── Type: [Dropdown: None/Bearer/Basic/OAuth2/...]
│   └─��� [Auth Config Fields]
│
├── Headers
│   ���── [Key-Value Editor]
│   └─��� [+ Add Header]
│
├─��� Variables
���   ├���─ [Key-Value Editor with Type]
│   └���─ [+ Add Variable]
│
���── Scripts
    ├─��� Pre-Request  [Code Editor]
    └���─ Tests        [Code Editor]
```

### 4.3 ���件夹设���

**���口**：右���文件��� → Folder Settings

**UI**：与集合���置类���，但���包含 General ���面

```
Folder Settings Dialog (Modal)

├─��� Auth
├── Headers
├── Variables
└���─ Scripts
```

### 4.4 请求面板 Scripts Tab

```
Request Panel

├── Body
���── Headers
├─��� Auth
├���─ Scripts  ← 新增
│   ├── Pre-Request  [Code Editor]
│   └���─ Tests        [Code Editor]
└─��� Settings
```

## 5. pm API 设���

### 5.1 核心 API

```typescript
interface PostmanAPI {
  // 变量���作
  variables: {
    get(key: string): any;
    set(key: string, value: any): void;
    has(key: string): boolean;
    clear(key: string): void;
  };
  
  // 集合变量
  collectionVariables: {
    get(key: string): any;
    set(key: string, value: any): void;
  };
  
  // 环境���量
  environment: {
    get(key: string): any;
    set(key: string, value: any): void;
  };
  
  // 全局变量
  globals: {
    get(key: string): any;
    set(key: string, value: any): void;
  };
  
  // 请���对象
  request: {
    url: string;
    method: string;
    headers: Record<string, string>;
    body: any;
  };
  
  // ���应对象（��� Post-Response）
  response: {
    code: number;
    status: string;
    headers: Record<string, string>;
    body: string;
    responseTime: number;
    json(): any;
    text(): string;
  };
  
  // 测试 API
  test(name: string, fn: () => void): void;
  expect(value: any): Assertion;
  
  // 发送���外请���
  sendRequest(request: RequestConfig, callback: (err: any, response: any) => void): void;
}

interface Assertion {
  to: {
    equal(expected: any): void;
    be: {
      below(value: number): void;
      above(value: number): void;
      within(min: number, max: number): void;
      true: void;
      false: void;
    };
    have: {
      status(code: number): void;
      property(name: string): void;
    };
    exist: void;
  };
}
```

### 5.2 使���示例

**Pre-Request Script**:
```javascript
// 设置���间戳
pm.variables.set("timestamp", Date.now());

// 生成随机 ID
pm.variables.set("requestId", pm.variables.replaceIn("{{$guid}}"));

// 从环境变量获��� token
const token = pm.environment.get("auth_token");
if (!token) {
  throw new Error("Auth token not found");
}

// 修���请求
pm.request.headers["X-Request-ID"] = pm.variables.get("requestId");
```

**Post-Response Script (Tests)**:
```javascript
// 状态码���试
pm.test("Status code is 200", function () {
  pm.response.to.have.status(200);
});

// 响应���间测���
pm.test("Response time < 200ms", function () {
  pm.expect(pm.response.responseTime).to.be.below(200);
});

// JSON 结构���试
pm.test("Response has valid structure", function () {
  const json = pm.response.json();
  pm.expect(json).to.have.property("code");
  pm.expect(json).to.have.property("data");
  pm.expect(json.code).to.equal(0);
});

// 提���数据到���量
const json = pm.response.json();
if (json.data && json.data.token) {
  pm.collectionVariables.set("auth_token", json.data.token);
}
```

## 6. 实现优���级

### Phase 1: ���据结构 + ���础持���化
- [ ] 扩展 TypeScript 类型定���
- [ ] ���新 Rust struct
- [ ] 修改 collection-store.ts 持久化���辑
- [ ] ���试数据保���和加���

### Phase 2: 请求级 Scripts
- [ ] 实现 pm API（基础版���
- [ ] 实现脚���执行���擎
- [ ] Request Panel 添��� Scripts Tab
- [ ] 测��� Pre-Request 和 Post-Response

### Phase 3: 配置���承逻辑
- [ ] 实��� Headers 合并
- [ ] 实��� Auth 继承
- [ ] 实��� Variables 解析
- [ ] 实现 Scripts ���行顺序

### Phase 4: 集合���配置 UI
- [ ] 集���设置 Tab 页面
- [ ] General 配置���
- [ ] Auth 配置页
- [ ] Headers 配置页
- [ ] Variables 配置页
- [ ] Scripts ���置页

### Phase 5: 文件夹级���置
- [ ] 文件���设置 Dialog
- [ ] 文���夹配置继���

### Phase 6: 测试���果展示
- [ ] Response Panel Tests Tab
- [ ] ���试结���列表
- [ ] 测试���计

### Phase 7: Collection Runner
- [ ] ���量运���集合
- [ ] 测试报告���成

## 7. 文件���单

���要修改/创���的文���：

```
packages/types/src/
├���─ index.ts                          # 添加新类���定义

apps/desktop/src-tauri/src/commands/
├── collection.rs                     # 更新 Rust struct

apps/desktop/src/stores/
├─��� collection-store.ts               # 更���持久化逻辑

apps/desktop/src/components/
├���─ collection/
│   ├── CollectionSettingsTab.tsx    # 新建：���合设��� Tab
│   ���── FolderSettingsDialog.tsx     # 新建：文���夹设���弹窗
│   └─��� ScriptEditor.tsx              # 新建���脚本���辑器
├── request/
│   └���─ ScriptsTab.tsx                # 新建���请求���本 Tab
└── response/
    └── TestsTab.tsx                  # 新���：测���结果 Tab

apps/desktop/src/lib/
├── pm-api.ts                         # 新建���pm API 实现
├── script-executor.ts                # 新建：���本执行���擎
���── config-resolver.ts                # 新建：���置继���解析器
```

---

**下一���**：���始实现 Phase 1 - 数据结构���展
