# API Client 请求编辑视觉规范

> 本文档拆分自原 07-核心页面视觉规范.md，涵盖请求编辑页（Request Editor Page）视觉规范。其他页面视觉规范见 07a/07c。
> CSS 变量命名规范及设计 Token 速查见 07a-首页视觉规范.md。

## 2. 请求编辑页（Request Editor Page）

> 这是应用最核心、最复杂的页面，规范最为详细。

### 2.1 布局总览

```
┌──────────┬──────────────────────────────────────────────────────────────────────────────┐
│          │ [≡] [←][→]  [ GET ▼ ]  [ https://api.example.com/{{base}}/users  ]  [ ▶ ]  │ 44px URL栏
│          │              [ Dev ▼ ]  [⌘K]                                   [ ⚙ ▼ ]  │
│  Side    ├──────────────────────────────────────────────────────────────────────────────┤
│  bar     │ [×] [GET /users] [POST /orders] [+]                              [⋮][⌘\][⛶]│ 36px Tab Bar
│          ├──────────────────────────────────────────────────────────────────────────────┤
│  220px   │ [ Params ] [ Headers ] [ Body ] [ Auth ] [ Scripts ]                         │ 36px 请求Tab
│          ├──────────────────────────────────────────────────────────────────────────────┤
│          │                                                                              │
│          │                      请求编辑内容区                                             │
│          │                      （高度可变，由分屏条分割）                                  │
│          ├─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤ 4px 分割条
│          │ [200 OK] [245ms] [1.2KB]                     [⌘\ 分屏] [⛶ 全屏]             │ 32px 响应状态
│          ├──────────────────────────────────────────────────────────────────────────────┤
│          │ [ Body ] [ Headers ] [ Cookies ] [ Tests ]                                  │ 36px 响应Tab
│          ├──────────────────────────────────────────────────────────────────────────────┤
│          │                                                                              │
│          │                      响应查看内容区                                             │
│          │                                                                              │
│          └──────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 URL 栏

```css
.url-bar {
  display: flex;
  align-items: center;
  height: 44px;
  padding: 0 8px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  gap: 4px;
}

.url-bar-toggle {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms var(--ease-default);
  flex-shrink: 0;
}

.url-bar-toggle:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.url-bar-toggle:active {
  background: var(--bg-active);
}

.url-bar-nav {
  display: flex;
  gap: 2px;
}

.url-bar-nav-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms var(--ease-default);
}

.url-bar-nav-btn:hover:not(:disabled) {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.url-bar-nav-btn:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}

.url-bar-separator {
  width: 1px;
  height: 20px;
  background: var(--border-muted);
  margin: 0 4px;
  flex-shrink: 0;
}

.method-selector {
  position: relative;
  flex-shrink: 0;
}

.method-selector-trigger {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 32px;
  padding: 0 10px 0 12px;
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 600;
  line-height: 16px;
  cursor: pointer;
  transition: all 100ms var(--ease-default);
  user-select: none;
}

.method-selector-trigger.get    { color: var(--method-get);    background: rgba(0, 214, 143, 0.12); }
.method-selector-trigger.post   { color: var(--method-post);   background: rgba(255, 184, 0, 0.12); }
.method-selector-trigger.put    { color: var(--method-put);    background: rgba(91, 141, 239, 0.12); }
.method-selector-trigger.patch  { color: var(--method-patch);  background: rgba(191, 90, 242, 0.12); }
.method-selector-trigger.delete { color: var(--method-delete); background: rgba(255, 71, 87, 0.12); }

.method-selector-trigger:hover {
  filter: brightness(1.15);
}

.method-selector-trigger .chevron {
  width: 12px;
  height: 12px;
  opacity: 0.5;
  transition: transform 100ms var(--ease-default);
}

.method-selector.open .chevron {
  transform: rotate(180deg);
}

.method-dropdown {
  position: absolute;
  top: calc(100% + 4px);
  left: 0;
  width: 160px;
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  border-radius: 8px;
  padding: 4px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  z-index: var(--z-dropdown);
  animation: fadeInUp 100ms var(--ease-smooth);
}

.method-dropdown-item {
  display: flex;
  align-items: center;
  height: 30px;
  padding: 0 10px;
  border-radius: 4px;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: background 50ms;
}

.method-dropdown-item:hover {
  background: var(--bg-hover);
}

.method-dropdown-item.active {
  background: var(--brand-muted);
}

.url-input-wrapper {
  flex: 1;
  height: 32px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  transition: border-color 100ms var(--ease-default);
}

.url-input-wrapper:focus-within {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.url-input {
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
}

.url-input::placeholder {
  color: var(--fg-tertiary);
}

.url-input .variable {
  color: var(--brand);
  background: var(--brand-muted);
  border-radius: 2px;
  padding: 0 2px;
  cursor: pointer;
}

.url-input .variable:hover {
  background: rgba(108, 92, 231, 0.25);
}

.send-btn {
  width: 40px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  background: var(--brand);
  color: #FFFFFF;
  cursor: pointer;
  flex-shrink: 0;
  transition: all 100ms var(--ease-default);
}

.send-btn:hover {
  background: var(--brand-hover);
  box-shadow: 0 0 20px rgba(108, 92, 231, 0.3);
  transform: scale(1.02);
}

.send-btn:active {
  transform: scale(0.98);
  background: var(--brand);
  box-shadow: none;
}

.send-btn.sending {
  animation: pulseGlow 1.5s ease-in-out infinite;
  pointer-events: none;
}

.send-btn.completed {
  background: var(--accent-success);
  transition: background 100ms;
}

.send-btn.completed::after {
  content: '\2713';
  font-size: 12px;
  font-weight: 700;
}

.send-btn.completed.returning {
  background: var(--brand);
  transition: background 180ms var(--ease-default);
}

.send-btn.error {
  background: var(--accent-danger);
}

.send-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
}

@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 12px rgba(108, 92, 231, 0.3); }
  50%       { box-shadow: 0 0 24px rgba(108, 92, 231, 0.6); }
}
```

### 2.3 URL 栏第二行

```css
.url-bar-row2 {
  display: flex;
  align-items: center;
  height: 28px;
  padding: 0 8px;
  gap: 8px;
}

.env-selector {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 22px;
  padding: 0 10px;
  border-radius: 9999px;
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  line-height: 14px;
  cursor: pointer;
  transition: all 100ms var(--ease-default);
  user-select: none;
}

.env-selector.development {
  color: var(--accent-success);
  background: rgba(0, 214, 143, 0.1);
  border: 1px solid rgba(0, 214, 143, 0.2);
}

.env-selector.staging {
  color: var(--accent-warning);
  background: rgba(255, 184, 0, 0.1);
  border: 1px solid rgba(255, 184, 0, 0.2);
}

.env-selector.production {
  color: var(--accent-danger);
  background: rgba(255, 71, 87, 0.1);
  border: 1px solid rgba(255, 71, 87, 0.2);
}

.env-selector:hover {
  filter: brightness(1.2);
}

.env-selector-dot {
  width: 6px;
  height: 6px;
  border-radius: 9999px;
  background: currentColor;
}

.url-bar-cmd {
  width: 28px;
  height: 22px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  font-size: 10px;
  font-weight: 500;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.url-bar-cmd:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.url-bar-menu {
  margin-left: auto;
  width: 28px;
  height: 22px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.url-bar-menu:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}
```

### 2.4 Tab 栏

```css
.tab-bar {
  display: flex;
  align-items: center;
  height: 36px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  padding: 0 4px;
  overflow-x: auto;
  overflow-y: hidden;
  scrollbar-width: none;
}

.tab-bar::-webkit-scrollbar { display: none; }

.tab-item {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 12px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-secondary);
  line-height: 16px;
  cursor: pointer;
  white-space: nowrap;
  max-width: 180px;
  min-width: 80px;
  transition: all 50ms var(--ease-default);
  position: relative;
  flex-shrink: 0;
}

.tab-item:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.tab-item.active {
  color: var(--fg-primary);
  background: var(--bg-hover);
}

.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 8px;
  right: 8px;
  height: 2px;
  background: var(--brand);
  border-radius: 1px;
}

.tab-item-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
}

.tab-item-method.get    { color: var(--method-get); }
.tab-item-method.post   { color: var(--method-post); }
.tab-item-method.put    { color: var(--method-put); }
.tab-item-method.patch  { color: var(--method-patch); }
.tab-item-method.delete { color: var(--method-delete); }

.tab-item-label {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.tab-item-close {
  width: 16px;
  height: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  opacity: 0;
  color: var(--fg-tertiary);
  transition: all 50ms;
  flex-shrink: 0;
}

.tab-item:hover .tab-item-close { opacity: 1; }

.tab-item-close:hover {
  background: var(--bg-active);
  color: var(--fg-primary);
}

.tab-item-dirty {
  width: 6px;
  height: 6px;
  border-radius: 9999px;
  background: var(--accent-warning);
  flex-shrink: 0;
}

.tab-item-dirty + .tab-item-close { display: none; }
.tab-item:hover .tab-item-dirty { display: none; }
.tab-item:hover .tab-item-close { display: flex; }

.tab-bar-add {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
  flex-shrink: 0;
}

.tab-bar-add:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.tab-bar-tools {
  margin-left: auto;
  display: flex;
  gap: 2px;
  flex-shrink: 0;
  padding-left: 8px;
}

.tab-bar-tool-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.tab-bar-tool-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.tab-scroll-btn {
  width: 20px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--fg-tertiary);
  cursor: pointer;
  flex-shrink: 0;
}

.tab-scroll-btn:hover { color: var(--fg-secondary); }
```

### 2.5 请求 Tab 栏

```css
.request-tabs {
  display: flex;
  align-items: center;
  height: 36px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  padding: 0 12px;
  gap: 0;
  position: relative;
}

.request-tab {
  height: 36px;
  padding: 0 14px;
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  line-height: 16px;
  cursor: pointer;
  white-space: nowrap;
  position: relative;
  transition: color 50ms;
}

.request-tab:hover { color: var(--fg-primary); }
.request-tab.active { color: var(--fg-primary); }

.request-tab-indicator {
  position: absolute;
  bottom: 0;
  height: 2px;
  background: var(--brand);
  border-radius: 1px;
  transition: left 180ms var(--ease-smooth),
              width 180ms var(--ease-smooth);
}

.request-tab-badge {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  min-width: 16px;
  height: 16px;
  padding: 0 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 9999px;
  color: var(--fg-tertiary);
  background: var(--bg-active);
}

.request-tab.active .request-tab-badge {
  color: var(--brand);
  background: var(--brand-muted);
}
```

### 2.6 Key-Value 编辑器

```css
.kv-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
  overflow: hidden;
}

.kv-editor-header {
  display: grid;
  grid-template-columns: 20px 200px 1fr 180px 28px;
  height: 28px;
  padding: 0 12px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.kv-editor-body {
  flex: 1;
  overflow-y: auto;
  padding: 0 12px;
}

.kv-row {
  display: grid;
  grid-template-columns: 20px 200px 1fr 180px 28px;
  height: 32px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.kv-row:hover { background: var(--bg-hover); }

.kv-row-checkbox {
  width: 14px;
  height: 14px;
  border-radius: 4px;
  border: 1.5px solid var(--border-default);
  background: transparent;
  cursor: pointer;
  transition: all 50ms;
  display: flex;
  align-items: center;
  justify-content: center;
}

.kv-row-checkbox.checked {
  background: var(--brand);
  border-color: var(--brand);
}

.kv-row-checkbox.checked::after {
  content: '\2713';
  font-size: 9px;
  color: #FFFFFF;
  font-weight: 700;
}

.kv-row.disabled .kv-row-checkbox { opacity: 0.35; }

.kv-row-key {
  height: 100%;
  display: flex;
  align-items: center;
  padding: 0 8px;
}

.kv-row-key input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
}

.kv-row-key input::placeholder { color: var(--fg-tertiary); }

.kv-row-value {
  height: 100%;
  display: flex;
  align-items: center;
  padding: 0 8px;
}

.kv-row-value input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
}

.kv-row-value .variable {
  color: var(--brand);
  background: var(--brand-muted);
  border-radius: 2px;
  padding: 0 2px;
}

.kv-row-desc {
  height: 100%;
  display: flex;
  align-items: center;
  padding: 0 8px;
}

.kv-row-desc input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-secondary);
  line-height: 16px;
}

.kv-row-desc input::placeholder { color: var(--fg-tertiary); }

.kv-row-delete {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  opacity: 0;
  transition: all 50ms;
}

.kv-row:hover .kv-row-delete { opacity: 1; }

.kv-row-delete:hover {
  background: rgba(255, 71, 87, 0.12);
  color: var(--accent-danger);
}

.kv-row.disabled { opacity: 0.4; }
.kv-row.disabled input { text-decoration: line-through; }

.kv-editor-add {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 32px;
  padding: 0 12px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.kv-editor-add:hover {
  color: var(--brand);
  background: var(--brand-muted);
}

.kv-editor-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 8px;
  color: var(--fg-tertiary);
  font-size: 12px;
}
```

### 2.7 Body 编辑器

```css
.body-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.body-type-row {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 36px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
}

.body-type-radio {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 24px;
  padding: 0 10px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.body-type-radio:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.body-type-radio.active {
  color: var(--brand);
  background: var(--brand-muted);
}

.body-type-dot {
  width: 10px;
  height: 10px;
  border-radius: 9999px;
  border: 1.5px solid var(--border-default);
  transition: all 100ms;
}

.body-type-radio.active .body-type-dot {
  border-color: var(--brand);
  background: var(--brand);
  box-shadow: inset 0 0 0 2px var(--bg-surface);
}

.body-type-separator {
  width: 1px;
  height: 16px;
  background: var(--border-muted);
}

.raw-type-selector {
  display: flex;
  align-items: center;
  height: 24px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  gap: 4px;
  transition: all 50ms;
}

.raw-type-selector:hover { background: var(--bg-hover); }

.body-code-editor {
  flex: 1;
  overflow: hidden;
  position: relative;
}

.body-code-editor .monaco-editor { padding: 8px 0; }

.form-data-row {
  display: grid;
  grid-template-columns: 20px 200px 80px 1fr 28px;
  height: 32px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.form-data-row:hover { background: var(--bg-hover); }

.form-data-type-select {
  height: 24px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  background: var(--bg-active);
  cursor: pointer;
  border: none;
  outline: none;
}

.binary-upload-zone {
  width: 280px;
  height: 160px;
  border: 1.5px dashed var(--border-default);
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  cursor: pointer;
  transition: all 180ms var(--ease-default);
}

.binary-upload-zone:hover {
  border-color: var(--brand);
  background: var(--brand-muted);
}

.binary-upload-zone.dragover {
  border-color: var(--brand);
  background: rgba(108, 92, 231, 0.1);
  box-shadow: 0 0 20px rgba(108, 92, 231, 0.15);
}

.graphql-editor {
  display: grid;
  grid-template-columns: 1fr 1fr;
  height: 100%;
  gap: 0;
}

.graphql-editor-query {
  border-right: 1px solid var(--border-muted);
}
```

### 2.8 Auth 面板

``````css
.auth-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 16px 12px;
  gap: 12px;
}

.auth-type-select {
  width: 220px;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-primary);
  cursor: pointer;
  transition: border-color 100ms;
}

.auth-type-select:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.auth-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.auth-field-label {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-secondary);
  line-height: 16px;
}

.auth-field-input {
  width: 100%;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.auth-field-input:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.auth-field-input::placeholder {
  color: var(--fg-tertiary);
  font-family: var(--font-sans);
}

.auth-field-input.password { padding-right: 36px; }

.auth-hint {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  line-height: 16px;
  padding: 8px 12px;
  background: var(--bg-elevated);
  border-radius: 6px;
  border-left: 2px solid var(--accent-info);
}
``````

### 2.9 Scripts 面板

``````css
.scripts-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.scripts-tabs {
  display: flex;
  height: 36px;
  border-bottom: 1px solid var(--border-muted);
  padding: 0 12px;
  gap: 0;
  position: relative;
}

.scripts-tab {
  height: 36px;
  padding: 0 14px;
  display: flex;
  align-items: center;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: color 50ms;
}

.scripts-tab:hover { color: var(--fg-primary); }
.scripts-tab.active { color: var(--fg-primary); }

.scripts-code-area { flex: 1; overflow: hidden; }

.scripts-snippet-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 10px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.scripts-snippet-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}
``````

### 2.10 分割条

``````css
.split-handle {
  height: 4px;
  background: transparent;
  transition: background 50ms;
  cursor: row-resize;
  position: relative;
  flex-shrink: 0;
}

.split-handle::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 32px;
  height: 2px;
  border-radius: 1px;
  background: transparent;
  transition: background 100ms;
}

.split-handle:hover { background: var(--bg-hover); }
.split-handle:hover::after { background: var(--fg-tertiary); }
.split-handle:active { background: var(--brand); }
.split-handle:active::after { background: #FFFFFF; }
.split-handle.dragging { background: var(--brand); }
``````

### 2.11 响应状态条

``````css
.response-bar {
  display: flex;
  align-items: center;
  height: 32px;
  padding: 0 12px;
  gap: 8px;
  background: var(--bg-surface);
  border-top: 1px solid var(--border-muted);
  border-bottom: 1px solid var(--border-muted);
}

.response-status-pill {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 600;
  line-height: 14px;
}

.response-status-pill.s2xx { color: var(--accent-success); background: rgba(0, 214, 143, 0.12); }
.response-status-pill.s3xx { color: var(--accent-info);    background: rgba(91, 141, 239, 0.12); }
.response-status-pill.s4xx { color: var(--accent-warning); background: rgba(255, 184, 0, 0.12); }
.response-status-pill.s5xx { color: var(--accent-danger);  background: rgba(255, 71, 87, 0.12); }

.response-meta {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 400;
  color: var(--fg-tertiary);
  line-height: 14px;
}

.response-bar-tools {
  margin-left: auto;
  display: flex;
  gap: 2px;
}

.response-bar-btn {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.response-bar-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.response-bar-btn.active {
  color: var(--brand);
  background: var(--brand-muted);
}
``````

### 2.12 响应 Tab 栏

``````css
/* 复用 .request-tabs / .request-tab 样式体系 */
.response-tabs {
  display: flex;
  align-items: center;
  height: 36px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  padding: 0 12px;
  gap: 0;
  position: relative;
}
``````

### 2.13 JSON 查看器

``````css
.json-viewer {
  padding: 8px 12px;
  font-family: var(--font-mono);
  font-size: 12px;
  line-height: 20px;
  overflow: auto;
  height: 100%;
}

.json-key     { color: #5B8DEF; }
.json-string  { color: #00D68F; }
.json-number  { color: #FFB800; }
.json-boolean { color: #BF5AF2; }
.json-null    { color: var(--fg-tertiary); font-style: italic; }
.json-bracket { color: var(--fg-tertiary); }

.json-line {
  padding: 0 0 0 calc(var(--indent) * 16px);
  transition: background 50ms;
  border-radius: 2px;
}

.json-line:hover { background: var(--bg-hover); }

.json-fold {
  cursor: pointer;
  color: var(--fg-tertiary);
  user-select: none;
}

.json-fold:hover { color: var(--fg-secondary); }

.json-preview {
  color: var(--fg-tertiary);
  font-style: italic;
  font-size: 11px;
  margin-left: 8px;
}
``````

### 2.14 HTML/Text 预览

``````css
.response-preview-toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 32px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
}

.response-preview-mode {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 24px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.response-preview-mode.active {
  color: var(--brand);
  background: var(--brand-muted);
}

.response-preview-mode:hover { background: var(--bg-hover); }
``````

### 2.15 Headers 列表

``````css
.response-header-row {
  display: grid;
  grid-template-columns: 240px 1fr;
  height: 28px;
  padding: 0 12px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.response-header-row:hover { background: var(--bg-hover); }

.response-header-key {
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 600;
  color: var(--fg-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.response-header-value {
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-secondary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
``````

### 2.16 Tests 结果

``````css
.test-result {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 28px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  transition: background 50ms;
}

.test-result:hover { background: var(--bg-hover); }

.test-result-icon.pass { color: var(--accent-success); }
.test-result-icon.fail { color: var(--accent-danger); }

.test-summary-pass {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--accent-success);
}

.test-summary-fail {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--accent-danger);
}
``````

### 2.17 响应区空状态

``````css
.response-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  text-align: center;
}

.response-empty-icon {
  width: 48px;
  height: 48px;
  color: var(--fg-tertiary);
  opacity: 0.3;
}

.response-empty-text {
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-tertiary);
}

.response-empty-shortcut {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--fg-tertiary);
  padding: 2px 8px;
  background: var(--bg-active);
  border-radius: 4px;
}
``````

### 2.18 响应区加载状态

``````css
.response-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 16px;
}

.response-loading-spinner {
  width: 24px;
  height: 24px;
  border: 2px solid var(--border-default);
  border-top-color: var(--brand);
  border-radius: 9999px;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.response-loading-timer {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--brand);
}
``````

### 2.19 响应区错误状态

``````css
.response-error {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  text-align: center;
  padding: 24px;
}

.response-error-icon {
  width: 40px;
  height: 40px;
  color: var(--accent-danger);
}

.response-error-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 500;
  color: var(--fg-primary);
}

.response-error-message {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--accent-danger);
  background: rgba(255, 71, 87, 0.08);
  padding: 8px 12px;
  border-radius: 6px;
  max-width: 480px;
  word-break: break-all;
}

.response-error-hint {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-tertiary);
}
``````

---

## 3. 命令面板（Command Palette）

### 3.1 布局总览

```
                        ┌─────────────────────────────────────────────┐
                        │  🔍  搜索请求、集合、变量、操作...           │  48px 搜索区
                        ├─────────────────────────────────────────────┤
                        │                                              │
                        │  最近                                        │
                        │  ┌───────────────────────────────────────┐ │
                        │  │ ▸ GET /users         My Collection     │ │  36px/行
                        │  │ ▸ POST /orders       My Collection     │ │
                        │  │ ▸ PUT /users/:id     Auth API          │ │
                        │  └───────────────────────────────────────┘ │
                        │                                              │
                        │  操作                                        │
                        │  ┌───────────────────────────────────────┐ │
                        │  │ ▹ 新建请求                ⌘N          │ │
                        │  │ ▹ 新建集合                ⌘⇧N         │ │
                        │  │ ▹ 切换环境 → Staging      ⌘E          │ │
                        │  │ ▹ 生成代码 (cURL)         ⌘⇧C         │ │
                        │  │ ▹ 查看变量                ⌘⇧V         │ │
                        │  │ ▹ 打开 Console            ⌘J          │ │
                        │  └───────────────────────────────────────┘ │
                        │                                              │
                        │  AI                                          │
                        │  ┌───────────────────────────────────────┐ │
                        │  │ ✦ 帮我测试这个 API                      │ │
                        │  │ ✦ 解释这个响应                         │ │
                        │  │ ✦ 从 OpenAPI 创建集合                   │ │
                        │  └───────────────────────────────────────┘ │
                        │                                              │
                        └─────────────────────────────────────────────┘
```

### 3.2 面板容器

``````css
.command-palette-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
  z-index: var(--z-command-palette);
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding-top: 20vh;
  animation: fadeIn var(--duration-fast) var(--ease-default);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}

.command-palette {
  width: 560px;
  max-height: 480px;
  background: var(--glass-bg);
  backdrop-filter: blur(20px);
  border: 1px solid var(--glass-border);
  border-radius: 12px;
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.5),
              0 0 0 1px rgba(255, 255, 255, 0.05);
  overflow: hidden;
  animation: cmdSlideIn 180ms var(--ease-smooth);
}

@keyframes cmdSlideIn {
  from {
    opacity: 0;
    transform: translateY(-8px) scale(0.98);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
``````

### 3.3 搜索输入区

``````css
.cmd-search {
  display: flex;
  align-items: center;
  gap: 10px;
  height: 48px;
  padding: 0 16px;
  border-bottom: 1px solid var(--border-muted);
}

.cmd-search-icon {
  width: 16px;
  height: 16px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.cmd-search-input {
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 20px;
}

.cmd-search-input::placeholder {
  color: var(--fg-tertiary);
}

.cmd-search-esc {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  color: var(--fg-tertiary);
  padding: 2px 6px;
  background: var(--bg-active);
  border-radius: 4px;
  flex-shrink: 0;
}
``````

### 3.4 结果列表

``````css
.cmd-results {
  overflow-y: auto;
  max-height: 432px;         /* 480 - 48 搜索区 */
  padding: 8px 0;
}

.cmd-group-title {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  padding: 8px 16px 4px 16px;
  line-height: 16px;
}

.cmd-item {
  display: flex;
  align-items: center;
  gap: 10px;
  height: 36px;
  padding: 0 16px;
  cursor: pointer;
  transition: background 50ms;
}

.cmd-item:hover,
.cmd-item.selected {
  background: var(--bg-hover);
}

.cmd-item.selected {
  background: var(--brand-muted);
}

.cmd-item-icon {
  width: 16px;
  height: 16px;
  color: var(--fg-secondary);
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.cmd-item.selected .cmd-item-icon {
  color: var(--brand);
}

.cmd-item-label {
  flex: 1;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 18px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.cmd-item-label .highlight {
  color: var(--brand);
  font-weight: 600;
}

.cmd-item-desc {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 400;
  color: var(--fg-tertiary);
  line-height: 14px;
  flex-shrink: 0;
  max-width: 140px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.cmd-item-shortcut {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  color: var(--fg-tertiary);
  padding: 2px 6px;
  background: var(--bg-active);
  border-radius: 4px;
  flex-shrink: 0;
}

.cmd-item-ai {
  color: var(--brand);
  font-weight: 500;
}
``````

### 3.5 底部提示栏

``````css
.cmd-footer {
  display: flex;
  align-items: center;
  gap: 16px;
  height: 32px;
  padding: 0 16px;
  border-top: 1px solid var(--border-muted);
  background: var(--bg-elevated);
}

.cmd-footer-hint {
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
}

.cmd-footer-key {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 18px;
  height: 18px;
  padding: 0 4px;
  border-radius: 3px;
  background: var(--bg-active);
  font-size: 10px;
  font-weight: 500;
  color: var(--fg-secondary);
}
``````

### 3.6 空搜索结果

``````css
.cmd-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 32px 16px;
  gap: 8px;
}

.cmd-empty-icon {
  width: 32px;
  height: 32px;
  color: var(--fg-tertiary);
  opacity: 0.4;
}

.cmd-empty-text {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-tertiary);
}
``````

### 3.7 交互规格

| 操作 | 行为 |
|------|------|
| ⌘K | 打开命令面板 |
| ESC | 关闭命令面板 |
| ↑↓ | 上下选择项目 |
| Enter | 执行选中项目 |
| Tab | 补全选中项目的路径 |
| 输入文字 | 实时模糊搜索，150ms 防抖 |
| 点击遮罩 | 关闭命令面板 |
| ⌘1-9 | 快速选择第 N 个项目 |

### 3.8 动画时序

| 动画 | 持续时间 | 缓动 | 说明 |
|------|----------|------|------|
| 遮罩淡入 | 100ms | ease-default | 背景渐暗 |
| 面板滑入 | 180ms | ease-smooth | 从上方 -8px 滑入 + scale 0.98→1 |
| 选中切换 | 50ms | ease-default | 高亮背景色切换 |
| 搜索结果更新 | 150ms | ease-smooth | 防抖后结果列表刷新 |
| 关闭 | 100ms | ease-in | 淡出 + 向上 8px |

---

## 4. 集合管理（Collection Management）— 侧边栏

### 4.1 侧边栏整体

``````css
.sidebar {
  width: 220px;
  height: 100%;
  background: var(--bg-surface);
  border-right: 1px solid var(--border-muted);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  flex-shrink: 0;
  transition: width 180ms var(--ease-smooth);
}

.sidebar.collapsed {
  width: 52px;
}
``````

### 4.2 搜索栏

``````css
.sidebar-search {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 44px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.sidebar-search-input {
  flex: 1;
  height: 28px;
  padding: 0 8px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.sidebar-search-input:focus {
  border-color: var(--border-focus);
}

.sidebar-search-input::placeholder {
  color: var(--fg-tertiary);
}

.sidebar-search-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
  flex-shrink: 0;
}

.sidebar-search-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}
``````

### 4.3 集合区域

``````css
.sidebar-section {
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.sidebar-section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 28px;
  padding: 0 12px;
  flex-shrink: 0;
  cursor: pointer;
  user-select: none;
}

.sidebar-section-title {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  line-height: 16px;
}

.sidebar-section-toggle {
  width: 16px;
  height: 16px;
  color: var(--fg-tertiary);
  transition: transform 100ms var(--ease-default);
}

.sidebar-section.collapsed .sidebar-section-toggle {
  transform: rotate(-90deg);
}

.sidebar-section.collapsed .sidebar-section-body {
  display: none;
}

.sidebar-section-body {
  flex: 1;
  overflow-y: auto;
  padding: 2px 6px;
}

.sidebar-section-actions {
  display: flex;
  gap: 2px;
  opacity: 0;
  transition: opacity 50ms;
}

.sidebar-section-header:hover .sidebar-section-actions {
  opacity: 1;
}
``````

### 4.4 集合树节点

``````css
.tree-item {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 8px;
  padding-left: calc(8px + var(--depth, 0) * 16px);
  border-radius: 4px;
  cursor: pointer;
  transition: background 50ms;
  position: relative;
}

.tree-item:hover {
  background: var(--bg-hover);
}

.tree-item.active {
  background: var(--brand-muted);
  color: var(--brand);
}

.tree-item-icon {
  width: 14px;
  height: 14px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
  transition: transform 100ms var(--ease-default);
}

.tree-item.expanded .tree-item-icon {
  transform: rotate(90deg);
}

.tree-item-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  padding: 1px 5px;
  border-radius: 9999px;
  flex-shrink: 0;
  line-height: 14px;
  min-width: 32px;
  text-align: center;
}

.tree-item-method.get    { color: var(--method-get);    background: rgba(0, 214, 143, 0.12); }
.tree-item-method.post   { color: var(--method-post);   background: rgba(255, 184, 0, 0.12); }
.tree-item-method.put    { color: var(--method-put);    background: rgba(91, 141, 239, 0.12); }
.tree-item-method.patch  { color: var(--method-patch);  background: rgba(191, 90, 242, 0.12); }
.tree-item-method.delete { color: var(--method-delete); background: rgba(255, 71, 87, 0.12); }

.tree-item-label {
  flex: 1;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.tree-item.active .tree-item-label {
  color: var(--brand);
}

.tree-item-actions {
  display: flex;
  gap: 2px;
  opacity: 0;
  transition: opacity 50ms;
}

.tree-item:hover .tree-item-actions {
  opacity: 1;
}

.tree-item-action {
  width: 18px;
  height: 18px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 3px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.tree-item-action:hover {
  background: var(--bg-active);
  color: var(--fg-primary);
}

.tree-item-folder-icon {
  width: 14px;
  height: 14px;
  color: var(--fg-secondary);
  flex-shrink: 0;
}

.tree-item-collection-icon {
  width: 14px;
  height: 14px;
  color: var(--brand);
  flex-shrink: 0;
  opacity: 0.7;
}
``````

### 4.5 History 区域

``````css
.history-group-title {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  padding: 6px 8px 2px 8px;
}

.history-item {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 28px;
  padding: 0 8px;
  border-radius: 4px;
  cursor: pointer;
  transition: background 50ms;
}

.history-item:hover { background: var(--bg-hover); }

.history-item-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  min-width: 32px;
}

.history-item-path {
  flex: 1;
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 400;
  color: var(--fg-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.history-item-time {
  font-family: var(--font-sans);
  font-size: 10px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.history-item-status {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  flex-shrink: 0;
}

.history-item-status.s2xx { color: var(--accent-success); }
.history-item-status.s4xx { color: var(--accent-warning); }
.history-item-status.s5xx { color: var(--accent-danger); }
``````

### 4.6 Environments 区域

``````css
.env-item {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 28px;
  padding: 0 8px;
  border-radius: 4px;
  cursor: pointer;
  transition: background 50ms;
}

.env-item:hover { background: var(--bg-hover); }

.env-item.active { background: var(--brand-muted); }

.env-item-dot {
  width: 6px;
  height: 6px;
  border-radius: 9999px;
  flex-shrink: 0;
}

.env-item-dot.active {
  background: var(--brand);
  box-shadow: 0 0 6px rgba(108, 92, 231, 0.4);
}

.env-item-dot.inactive {
  background: var(--border-default);
}

.env-item-name {
  flex: 1;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
}

.env-item.active .env-item-name {
  color: var(--brand);
  font-weight: 500;
}

.env-item-color {
  width: 4px;
  height: 16px;
  border-radius: 2px;
  flex-shrink: 0;
}
``````

### 4.7 折叠态侧边栏

``````css
.sidebar.collapsed .sidebar-search-input { display: none; }
.sidebar.collapsed .sidebar-section-title { display: none; }
.sidebar.collapsed .sidebar-section-body { display: none; }
.sidebar.collapsed .tree-item-label { display: none; }

.sidebar-collapsed-item {
  width: 52px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.sidebar-collapsed-item:hover {
  color: var(--fg-primary);
  background: var(--bg-hover);
}

.sidebar-collapsed-item.active {
  color: var(--brand);
}
``````

---

## 5. 环境管理（Environment Editor）

### 5.1 布局 — 右侧抽屉

```
┌──────────────────────────────────────────────────┐
│  Environment: Development               [✕]     │ 48px 标题栏
├──────────────────────────────────────────────────┤
│                                                   │
│  ┌── Variables ─────────────────────────────┐     │
│  │  KEY            VALUE           TYPE      │     │
│  │  ─────────────────────────────────────── │     │
│  │  base_url       https://api.dev  env     │     │
│  │  api_key        ****              vault   │     │
│  │  timeout        30000             env     │     │
│  │                                          │     │
│  │  + Add Variable                          │     │
│  └──────────────────────────────────────────┘     │
│                                                   │
│  ┌── Computed ──────────────────────────────┐     │
│  │  full_url  = {{base_url}}/v1            │     │
│  │  → https://api.dev/v1                   │     │
│  └──────────────────────────────────────────┘     │
│                                                   │
│                          [Delete] [Save]          │ 56px 底部栏
└──────────────────────────────────────────────────┘
```

### 5.2 抽屉容器

``````css
.env-drawer-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: var(--z-overlay);
}

.env-drawer {
  position: fixed;
  top: 0;
  right: 0;
  width: 480px;
  height: 100vh;
  background: var(--bg-surface);
  border-left: 1px solid var(--border-muted);
  box-shadow: -8px 0 32px rgba(0, 0, 0, 0.4);
  z-index: var(--z-modal);
  display: flex;
  flex-direction: column;
  animation: slideInRight 180ms var(--ease-smooth);
}

@keyframes slideInRight {
  from { transform: translateX(100%); opacity: 0; }
  to   { transform: translateX(0); opacity: 1; }
}
``````

### 5.3 标题栏

``````css
.env-drawer-header {
  display: flex;
  align-items: center;
  gap: 12px;
  height: 48px;
  padding: 0 20px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.env-drawer-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 600;
  color: var(--fg-primary);
  line-height: 20px;
}

.env-drawer-name {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 500;
  color: var(--brand);
}

.env-drawer-close {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
  margin-left: auto;
}

.env-drawer-close:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}
``````

### 5.4 变量编辑区

``````css
.env-drawer-body {
  flex: 1;
  overflow-y: auto;
  padding: 16px 20px;
}

.env-var-section-title {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin: 0 0 12px 0;
}

.env-var-header {
  display: grid;
  grid-template-columns: 160px 1fr 80px 28px;
  height: 24px;
  padding: 0 0 0 8px;
  align-items: center;
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  border-bottom: 1px solid var(--border-muted);
}

.env-var-row {
  display: grid;
  grid-template-columns: 160px 1fr 80px 28px;
  height: 32px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.env-var-row:hover { background: var(--bg-hover); }

.env-var-key input {
  width: 100%;
  height: 100%;
  padding: 0 8px;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
}

.env-var-value input {
  width: 100%;
  height: 100%;
  padding: 0 8px;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
}

.env-var-type {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  padding: 2px 6px;
  border-radius: 4px;
  margin: 0 8px;
}

.env-var-type.env  { color: var(--accent-info);    background: rgba(91, 141, 239, 0.12); }
.env-var-type.vault { color: var(--accent-warning); background: rgba(255, 184, 0, 0.12); }
.env-var-type.local { color: var(--fg-tertiary);   background: var(--bg-active); }

.env-var-delete {
  width: 20px;
  height: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 3px;
  color: var(--fg-tertiary);
  cursor: pointer;
  opacity: 0;
  transition: all 50ms;
}

.env-var-row:hover .env-var-delete { opacity: 1; }

.env-var-delete:hover {
  background: rgba(255, 71, 87, 0.12);
  color: var(--accent-danger);
}
``````

### 5.5 Computed Variables 区域

``````css
.env-computed-section {
  margin-top: 24px;
}

.env-computed-row {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 8px 12px;
  background: var(--bg-elevated);
  border-radius: 6px;
  margin-bottom: 8px;
}

.env-computed-expr {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
}

.env-computed-expr .variable {
  color: var(--brand);
  background: var(--brand-muted);
  border-radius: 2px;
  padding: 0 2px;
}

.env-computed-result {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--accent-success);
}

.env-computed-result.error {
  color: var(--accent-danger);
}
``````

### 5.6 底部操作栏

``````css
.env-drawer-footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
  height: 56px;
  padding: 0 20px;
  border-top: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.env-btn-delete {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: transparent;
  border: 1px solid var(--border-default);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--accent-danger);
  cursor: pointer;
  transition: all 100ms;
  margin-right: auto;
}

.env-btn-delete:hover {
  background: rgba(255, 71, 87, 0.12);
  border-color: var(--accent-danger);
}

.env-btn-save {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: var(--brand);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: #FFFFFF;
  cursor: pointer;
  transition: all 100ms;
}

.env-btn-save:hover {
  background: var(--brand-hover);
  box-shadow: 0 0 16px rgba(108, 92, 231, 0.25);
}

.env-btn-save:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.env-btn-save:active {
  transform: scale(0.98);
}
``````

### 5.7 状态变体

| 状态 | 名称输入 | 值输入 | 类型标签 |
|------|---------|--------|---------|
| 默认 | --fg-primary | --fg-primary | 根据 env/vault/local 着色 |
| 聚焦 | 底部 2px 蓝色边框 | 底部 2px 蓝色边框 | — |
| 空值 | placeholder: "Key" | placeholder: "Value" | — |
| 敏感值(vault) | 正常显示 | **** 遮罩 | vault 橙色标签 |
| 错误 | 红色边框 | 红色边框 + 错误提示 | — |
| 删除确认 | — | — | 行背景闪烁红色 |

---

## 6. Console 面板

### 6.1 布局总览

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  Console                                                    [🗑️] [✕]    │ 32px 标题栏
├──────────────────────────────────────────────────────────────────────────────┤
│  [All] [Request] [Response] [Script]                           [🔍]       │ 28px 筛选栏
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  [12:34:56] → GET https://api.example.com/users              200  245ms     │ 24px/行
│  [12:34:56] ← 200 OK                                         1.2KB          │
│  [12:34:57] → POST https://api.example.com/orders            201  312ms     │
│  [12:34:57] ⚙ Script: Setting auth header                                   │
│  [12:34:57] ← 201 Created                                    2.4KB          │
│  [12:34:58] ✗ Error: ECONNREFUSED                                           │
│                                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│  4 entries                                            ↕ Drag to resize      │ 24px 状态栏
└──────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 面板容器

``````css
.console-panel {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 260px;
  background: var(--bg-surface);
  border-top: 1px solid var(--border-muted);
  z-index: var(--z-sticky);
  display: flex;
  flex-direction: column;
  animation: slideUp 180ms var(--ease-smooth);
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to   { transform: translateY(0); }
}

.console-panel.hidden {
  animation: slideDown 180ms var(--ease-smooth) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to   { transform: translateY(100%); }
}
``````

### 6.3 标题栏

``````css
.console-header {
  display: flex;
  align-items: center;
  height: 32px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
  cursor: row-resize;
}

.console-title {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--fg-primary);
  line-height: 16px;
}

.console-header-actions {
  display: flex;
  gap: 2px;
  margin-left: auto;
}

.console-header-btn {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.console-header-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.console-header-btn.danger:hover {
  background: rgba(255, 71, 87, 0.12);
  color: var(--accent-danger);
}
``````

### 6.4 筛选栏

``````css
.console-filter {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 28px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.console-filter-btn {
  height: 22px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.console-filter-btn:hover {
  background: var(--bg-hover);
}

.console-filter-btn.active {
  color: var(--brand);
  background: var(--brand-muted);
}

.console-filter-search {
  margin-left: auto;
  width: 160px;
  height: 22px;
  padding: 0 8px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-primary);
  outline: none;
}

.console-filter-search:focus {
  border-color: var(--border-focus);
}
``````

### 6.5 日志行

``````css
.console-body {
  flex: 1;
  overflow-y: auto;
  padding: 4px 0;
  font-family: var(--font-mono);
  font-size: 12px;
  line-height: 20px;
}

.console-row {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 24px;
  padding: 0 12px;
  transition: background 50ms;
}

.console-row:hover { background: var(--bg-hover); }

.console-row-time {
  font-size: 10px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
  min-width: 56px;
}

.console-row-icon {
  width: 14px;
  height: 14px;
  flex-shrink: 0;
}

.console-row-icon.request { color: var(--accent-info); }
.console-row-icon.response { color: var(--accent-success); }
.console-row-icon.script  { color: var(--accent-warning); }
.console-row-icon.error    { color: var(--accent-danger); }

.console-row-direction {
  font-size: 11px;
  font-weight: 600;
  flex-shrink: 0;
  min-width: 12px;
}

.console-row-direction.send { color: var(--accent-info); }
.console-row-direction.recv { color: var(--accent-success); }

.console-row-message {
  flex: 1;
  color: var(--fg-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.console-row-extra {
  font-size: 11px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.console-row-status {
  font-size: 11px;
  font-weight: 600;
  flex-shrink: 0;
}

.console-row-status.s2xx { color: var(--accent-success); }
.console-row-status.s4xx { color: var(--accent-warning); }
.console-row-status.s5xx { color: var(--accent-danger); }

.console-row-error {
  color: var(--accent-danger);
}
``````

### 6.6 状态栏

``````css
.console-status {
  display: flex;
  align-items: center;
  height: 24px;
  padding: 0 12px;
  border-top: 1px solid var(--border-muted);
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.console-status-count {
  font-weight: 500;
  color: var(--fg-secondary);
}
``````

### 6.7 空状态

``````css
.console-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 8px;
}

.console-empty-icon {
  width: 32px;
  height: 32px;
  color: var(--fg-tertiary);
  opacity: 0.3;
}

.console-empty-text {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-tertiary);
}
``````

---

## 7. 设置页面（Settings Modal）

### 7.1 布局 — 全屏模态

```
                    ┌────────────────────────────────────────────────────────────┐
                    │                   Settings                         [✕]    │ 48px
                    ├──────────┬─────────────────────────────────────────────────┤
                    │          │                                                  │
                    │  General │  Theme                                           │
                    │  Proxy   │  [Dark ▼]                                       │
                    │  Fonts   │                                                  │
                    │  Data    │  Font Size                                       │
                    │  About   │  [13 ▼]                                         │
                    │          │                                                  │
                    │          │  Default Environment                             │
                    │          │  [Development ▼]                                │
                    │          │                                                  │
                    │          │  Auto Save                                       │
                    │          │  [● ] Toggle                                    │
                    │          │                                                  │
                    │          │  Request Timeout (ms)                            │
                    │          │  [30000      ]                                  │
                    │          │                                                  │
                    │          │  SSL Verification                                │
                    │          │  [● ] Toggle                                    │
                    │          │                                                  │
                    └──────────┴─────────────────────────────────────────────────┘
```

### 7.2 模态容器

``````css
.settings-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: var(--z-modal);
  display: flex;
  align-items: center;
  justify-content: center;
  animation: fadeIn 100ms var(--ease-default);
}

.settings-modal {
  width: 680px;
  max-height: 560px;
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.5);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: fadeInUp 180ms var(--ease-smooth);
}

@keyframes fadeInUp {
  from { transform: translateY(8px); opacity: 0; }
  to   { transform: translateY(0); opacity: 1; }
}
``````

### 7.3 标题栏

``````css
.settings-header {
  display: flex;
  align-items: center;
  height: 48px;
  padding: 0 20px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.settings-title {
  font-family: var(--font-sans);
  font-size: 16px;
  font-weight: 600;
  color: var(--fg-primary);
  line-height: 24px;
}

.settings-close {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
  margin-left: auto;
}

.settings-close:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}
``````

### 7.4 左侧导航

``````css
.settings-body {
  display: flex;
  flex: 1;
  overflow: hidden;
}

.settings-nav {
  width: 160px;
  padding: 12px 8px;
  border-right: 1px solid var(--border-muted);
  flex-shrink: 0;
  overflow-y: auto;
}

.settings-nav-item {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 32px;
  padding: 0 10px;
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.settings-nav-item:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.settings-nav-item.active {
  background: var(--brand-muted);
  color: var(--brand);
  font-weight: 500;
}

.settings-nav-icon {
  width: 16px;
  height: 16px;
  flex-shrink: 0;
}
``````

### 7.5 右侧内容区

``````css
.settings-content {
  flex: 1;
  overflow-y: auto;
  padding: 20px 24px;
}

.settings-section {
  margin-bottom: 24px;
}

.settings-section-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 600;
  color: var(--fg-primary);
  line-height: 20px;
  margin: 0 0 16px 0;
}
``````

### 7.6 设置项 — 下拉选择器

``````css
.settings-field {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 36px;
  padding: 0 0;
  gap: 16px;
}

.settings-field + .settings-field {
  margin-top: 12px;
}

.settings-field-label {
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 18px;
  flex: 1;
}

.settings-field-desc {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 400;
  color: var(--fg-tertiary);
  line-height: 16px;
  margin-top: 2px;
}

.settings-select {
  width: 180px;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-primary);
  cursor: pointer;
  outline: none;
  transition: border-color 100ms;
  flex-shrink: 0;
}

.settings-select:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}
``````

### 7.7 设置项 — Toggle 开关

``````css
.settings-toggle {
  position: relative;
  width: 36px;
  height: 20px;
  background: var(--bg-active);
  border-radius: 10px;
  cursor: pointer;
  transition: background 180ms var(--ease-default);
  flex-shrink: 0;
}

.settings-toggle::after {
  content: '';
  position: absolute;
  top: 2px;
  left: 2px;
  width: 16px;
  height: 16px;
  background: var(--fg-secondary);
  border-radius: 9999px;
  transition: all 180ms var(--ease-default);
}

.settings-toggle.active {
  background: var(--brand);
}

.settings-toggle.active::after {
  left: 18px;
  background: #FFFFFF;
}

.settings-toggle:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
``````

### 7.8 设置项 — 文本输入

``````css
.settings-input {
  width: 180px;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
  flex-shrink: 0;
}

.settings-input:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.settings-input::placeholder {
  color: var(--fg-tertiary);
  font-family: var(--font-sans);
}

.settings-input.error {
  border-color: var(--accent-danger);
  box-shadow: 0 0 0 3px rgba(255, 71, 87, 0.1);
}

.settings-field-error {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--accent-danger);
  margin-top: 4px;
}
``````

### 7.9 设置项 — 危险操作按钮

``````css
.settings-btn-danger {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: transparent;
  border: 1px solid rgba(255, 71, 87, 0.3);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--accent-danger);
  cursor: pointer;
  transition: all 100ms;
}

.settings-btn-danger:hover {
  background: rgba(255, 71, 87, 0.12);
  border-color: var(--accent-danger);
}
``````

### 7.10 About 页面

``````css
.settings-about {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24px 0;
  gap: 8px;
  text-align: center;
}

.settings-about-logo {
  width: 64px;
  height: 64px;
  color: var(--brand);
  margin-bottom: 8px;
}

.settings-about-name {
  font-family: var(--font-sans);
  font-size: 20px;
  font-weight: 700;
  color: var(--fg-primary);
  line-height: 28px;
}

.settings-about-version {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-tertiary);
}

.settings-about-desc {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-secondary);
  max-width: 320px;
}

.settings-about-links {
  display: flex;
  gap: 16px;
  margin-top: 12px;
}

.settings-about-link {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--brand);
  cursor: pointer;
  transition: color 100ms;
}

.settings-about-link:hover {
  color: var(--brand-hover);
}
``````

### 7.11 设置项状态变体汇总

| 状态 | Toggle | Select | Input | 危险按钮 |
|------|--------|--------|-------|---------|
| 默认 | 灰色底 + 灰色圆点 | 边框 --border-muted | 边框 --border-muted | 边框半透明红 |
| 聚焦 | — | 蓝色边框 + 外发光 | 蓝色边框 + 外发光 | — |
| 开启 | 品牌色底 + 白色圆点(右) | — | — | — |
| 关闭 | 灰色底 + 灰色圆点(左) | — | — | — |
| 禁用 | 整体 opacity:0.4 | opacity:0.4 | opacity:0.4 | opacity:0.4 |
| 错误 | — | — | 红色边框 + 错误文字 | — |
| 悬停 | — | 边框变亮 | 边框变亮 | 红色填充 + 红色边框 |

---

## 8. 历史面板（History Panel）— 右侧抽屉

### 8.1 布局总览

```
┌──────────────────────────────────────────────────┐
│  History                                         │ 48px 标题栏
│  [🔍 Search history...                ] [🗑️]    │
├──────────────────────────────────────────────────┤
│                                                   │
│  Today                                            │ 20px 分组标题
│  ┌──────────────────────────────────────────────┐ │
│  │ GET  /users          200  245ms   2m ago     │ │ 32px/行
│  │ POST /orders         201  312ms   5m ago     │ │
│  │ PUT  /users/:id      200  189ms   12m ago    │ │
│  └──────────────────────────────────────────────┘ │
│                                                   │
│  Yesterday                                        │
│  ┌──────────────────────────────────────────────┐ │
│  │ DELETE /sessions    204  98ms    1d ago      │ │
│  └──────────────────────────────────────────────┘ │
│                                                   │
│  This Week                                        │
│  ┌──────────────────────────────────────────────┐ │
│  │ GET  /health        200  45ms    3d ago      │ │
│  └──────────────────────────────────────────────┘ │
│                                                   │
├──────────────────────────────────────────────────┤
│  5 entries                    [Clear All]         │ 40px 底部栏
└──────────────────────────────────────────────────┘
```

### 8.2 抽屉容器

``````css
.history-drawer-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: var(--z-overlay);
}

.history-drawer {
  position: fixed;
  top: 0;
  right: 0;
  width: 480px;
  height: 100vh;
  background: var(--bg-surface);
  border-left: 1px solid var(--border-muted);
  box-shadow: -8px 0 32px rgba(0, 0, 0, 0.4);
  z-index: var(--z-modal);
  display: flex;
  flex-direction: column;
  animation: slideInRight 180ms var(--ease-smooth);
}
``````

### 8.3 标题栏与搜索区

``````css
.history-header {
  display: flex;
  align-items: center;
  gap: 12px;
  height: 48px;
  padding: 0 20px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.history-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 600;
  color: var(--fg-primary);
}

.history-close {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
  margin-left: auto;
}

.history-close:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.history-search-wrapper {
  padding: 8px 20px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.history-search {
  width: 100%;
  height: 28px;
  padding: 0 8px 0 28px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.history-search:focus {
  border-color: var(--border-focus);
}

.history-search::placeholder {
  color: var(--fg-tertiary);
}
``````

### 8.4 分组标题

``````css
.history-group-title {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  padding: 12px 20px 4px 20px;
  line-height: 16px;
}
``````

### 8.5 历史条目

``````css
.history-item {
  display: grid;
  grid-template-columns: 56px 1fr 48px 56px 56px;
  align-items: center;
  height: 32px;
  padding: 0 20px;
  gap: 8px;
  cursor: pointer;
  transition: background 50ms;
}

.history-item:hover {
  background: var(--bg-hover);
}

.history-item-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  padding: 1px 5px;
  border-radius: 9999px;
  text-align: center;
  min-width: 32px;
  line-height: 14px;
}

.history-item-method.get    { color: var(--method-get);    background: rgba(0, 214, 143, 0.12); }
.history-item-method.post   { color: var(--method-post);   background: rgba(255, 184, 0, 0.12); }
.history-item-method.put    { color: var(--method-put);    background: rgba(91, 141, 239, 0.12); }
.history-item-method.patch  { color: var(--method-patch);  background: rgba(191, 90, 242, 0.12); }
.history-item-method.delete { color: var(--method-delete); background: rgba(255, 71, 87, 0.12); }

.history-item-path {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--fg-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.history-item-status {
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 600;
  text-align: right;
}

.history-item-status.s2xx { color: var(--accent-success); }
.history-item-status.s4xx { color: var(--accent-warning); }
.history-item-status.s5xx { color: var(--accent-danger); }

.history-item-duration {
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--fg-tertiary);
  text-align: right;
}

.history-item-time {
  font-family: var(--font-sans);
  font-size: 10px;
  color: var(--fg-tertiary);
  text-align: right;
}
``````

### 8.6 底部栏

``````css
.history-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 40px;
  padding: 0 20px;
  border-top: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.history-footer-count {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
}

.history-footer-count strong {
  color: var(--fg-secondary);
  font-weight: 500;
}

.history-clear-btn {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--accent-danger);
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
  border-radius: 4px;
  transition: background 50ms;
}

.history-clear-btn:hover {
  background: rgba(255, 71, 87, 0.12);
}
``````

### 8.7 空状态与搜索无结果

``````css
.history-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48px 24px;
  gap: 8px;
}

.history-empty-icon {
  width: 40px;
  height: 40px;
  color: var(--fg-tertiary);
  opacity: 0.3;
}

.history-empty-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 500;
  color: var(--fg-primary);
}

.history-empty-desc {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-tertiary);
  text-align: center;
}

.history-no-results {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32px 24px;
  gap: 8px;
}

.history-no-results-icon {
  width: 32px;
  height: 32px;
  color: var(--fg-tertiary);
  opacity: 0.4;
}

.history-no-results-text {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-tertiary);
}
``````

### 8.8 清除确认对话框

``````css
.history-clear-dialog-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: calc(var(--z-modal) + 1);
  display: flex;
  align-items: center;
  justify-content: center;
}

.history-clear-dialog {
  width: 360px;
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow: var(--shadow-xl);
  overflow: hidden;
  animation: fadeInUp 180ms var(--ease-smooth);
}

.history-clear-dialog-header {
  padding: 20px 20px 0 20px;
}

.history-clear-dialog-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 600;
  color: var(--fg-primary);
  line-height: 20px;
}

.history-clear-dialog-body {
  padding: 8px 20px 20px 20px;
}

.history-clear-dialog-desc {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-secondary);
  line-height: 18px;
}

.history-clear-dialog-footer {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  padding: 12px 20px;
  border-top: 1px solid var(--border-muted);
}

.history-clear-dialog-cancel {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: transparent;
  border: 1px solid var(--border-default);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--fg-primary);
  cursor: pointer;
  transition: all 100ms;
}

.history-clear-dialog-cancel:hover {
  background: var(--bg-hover);
}

.history-clear-dialog-confirm {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: var(--accent-danger);
  border: none;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: #FFFFFF;
  cursor: pointer;
  transition: all 100ms;
}

.history-clear-dialog-confirm:hover {
  background: #e63e4e;
}
``````

### 8.9 交互规格

| 操作 | 行为 |
|------|------|
| 点击历史项 | 打开该请求到新 Tab，填充 method + URL + headers + body |
| 搜索输入 | 150ms 防抖后过滤 URL 和 method |
| Clear All | 弹出确认对话框，确认后清空全部历史 |
| 右键历史项 | 上下文菜单：打开、删除、复制 cURL |
| Esc | 关闭抽屉 |
| 双击 | 同单击（打开请求） |

---

## 9. 设置页面 — 完整子页

### 9.1 代理配置页（Proxy）

```
┌──────────────────────────────────────────────────────────┐
│  Proxy Settings                                          │
│                                                          │
│  ┌─── Global Proxy ────────────────────────────────┐     │
│  │  Enabled                                         │     │
│  │  [● ] Toggle                                     │     │
│  │                                                   │     │
│  │  Protocol          [HTTP ▼]                       │     │
│  │  Host              [127.0.0.1       ]            │     │
│  │  Port              [7890            ]            │     │
│  │  Auth              [None ▼]                       │     │
│  │  Username          [               ]  (if Auth)  │     │
│  │  Password          [••••••         ]  (if Auth)  │     │
│  └──────────────────────────────────────────────────┘     │
│                                                          │
│  ┌─── Proxy Bypass ───────────────────────────────┐     │
│  │  localhost, 127.0.0.1, *.local                  │     │
│  │  [Add bypass host...                      ]     │     │
│  └──────────────────────────────────────────────────┘     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

``````css
.settings-proxy-section {
  margin-bottom: 24px;
}

.settings-proxy-card {
  background: var(--bg-elevated);
  border: 1px solid var(--border-muted);
  border-radius: 8px;
  padding: 16px;
}

.settings-proxy-card-title {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--fg-secondary);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin-bottom: 12px;
}

.settings-proxy-row {
  display: flex;
  align-items: center;
  gap: 12px;
  min-height: 36px;
}

.settings-proxy-label {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-primary);
  min-width: 100px;
  flex-shrink: 0;
}

.settings-proxy-input {
  flex: 1;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.settings-proxy-input:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.settings-proxy-select {
  width: 140px;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-primary);
  cursor: pointer;
  outline: none;
}

.settings-bypass-textarea {
  width: 100%;
  min-height: 60px;
  padding: 8px 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  resize: vertical;
  line-height: 18px;
}

.settings-bypass-textarea:focus {
  border-color: var(--border-focus);
}
``````

### 9.2 数据管理页（Data）

```
┌──────────────────────────────────────────────────────────┐
│  Data Management                                         │
│                                                          │
│  ┌─── Storage ──────────────────────────────────────┐   │
│  │  Storage Location                                 │   │
│  │  /Users/name/Library/Application Support/API      │   │
│  │  [Open in Finder]                                │   │
│  │                                                   │   │
│  │  Storage Usage                                    │   │
│  │  ████████░░░░░░░░░░░░  24.3 MB / 100 MB         │   │
│  │  Collections: 12  |  History: 1,245              │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─── Export ───────────────────────────────────────┐   │
│  │                                                   │   │
│  │  [Export All Data]                                │   │
│  │  Export all collections, environments, and        │   │
│  │  globals as a ZIP archive.                        │   │
│  │                                                   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─── Import ───────────────────────────────────────┐   │
│  │                                                   │   │
│  │  [Import Data]                                    │   │
│  │  Import from a previously exported ZIP, or       │   │
│  │  Postman / OpenAPI / cURL file.                   │   │
│  │                                                   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─── Danger Zone ─────────────────────────────────┐   │
│  │  [Clear All History]    [Reset All Settings]     │   │
│  │  [Delete All Data]                               │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

``````css
.settings-data-card {
  background: var(--bg-elevated);
  border: 1px solid var(--border-muted);
  border-radius: 8px;
  padding: 16px;
  margin-bottom: 16px;
}

.settings-data-card-title {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--fg-secondary);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin-bottom: 12px;
}

.settings-data-path {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
}

.settings-data-path-text {
  flex: 1;
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--fg-tertiary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.settings-data-path-btn {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--brand);
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
  border-radius: 4px;
  transition: background 50ms;
  flex-shrink: 0;
}

.settings-data-path-btn:hover {
  background: var(--brand-muted);
}

.settings-data-usage-bar {
  width: 100%;
  height: 6px;
  background: var(--bg-active);
  border-radius: 3px;
  overflow: hidden;
  margin-bottom: 4px;
}

.settings-data-usage-fill {
  height: 100%;
  background: var(--brand);
  border-radius: 3px;
  transition: width 300ms var(--ease-smooth);
}

.settings-data-usage-text {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--fg-tertiary);
  margin-bottom: 4px;
}

.settings-data-usage-detail {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
}

.settings-data-action-btn {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  height: 36px;
  padding: 0 12px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--fg-primary);
  cursor: pointer;
  transition: all 100ms;
  margin-bottom: 8px;
}

.settings-data-action-btn:hover {
  border-color: var(--brand);
  color: var(--brand);
}

.settings-data-action-desc {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  margin-top: -4px;
  margin-bottom: 12px;
  padding-left: 4px;
}

.settings-danger-zone {
  background: var(--bg-elevated);
  border: 1px solid rgba(255, 71, 87, 0.15);
  border-radius: 8px;
  padding: 16px;
}

.settings-danger-zone-title {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--accent-danger);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin-bottom: 12px;
}

.settings-danger-btn {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: transparent;
  border: 1px solid rgba(255, 71, 87, 0.3);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--accent-danger);
  cursor: pointer;
  transition: all 100ms;
  margin-right: 8px;
  margin-bottom: 8px;
}

.settings-danger-btn:hover {
  background: rgba(255, 71, 87, 0.12);
  border-color: var(--accent-danger);
}
``````

### 9.3 字体配置页（Fonts）

```
┌──────────────────────────────────────────────────────────┐
│  Font Settings                                           │
│                                                          │
│  Interface Font                                          │
│  [Geist                                     ] [Reset]    │
│                                                          │
│  Code Font                                               │
│  [JetBrains Mono                           ] [Reset]    │
│                                                          │
│  Font Size                                               │
│  [13 ▼]                                                  │
│                                                          │
│  Code Font Size                                          │
│  [12 ▼]                                                  │
│                                                          │
│  Line Height                                             │
│  [1.5 ▼]                                                 │
│                                                          │
│  Font Smoothing                                          │
│  [● ] Auto-smooth fonts                                 │
│                                                          │
│  Ligatures                                               │
│  [● ] Enable code ligatures                             │
│                                                          │
│  ┌─── Preview ──────────────────────────────────────┐   │
│  │  The quick brown fox jumps over the lazy dog.    │   │
│  │  0123456789 !@#$%^&*()                            │   │
│  │  => !== === >= <= ->                              │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

``````css
.settings-font-input {
  width: 100%;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.settings-font-input:focus {
  border-color: var(--border-focus);
}

.settings-font-reset {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-tertiary);
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 4px 8px;
  border-radius: 4px;
  transition: all 50ms;
  flex-shrink: 0;
}

.settings-font-reset:hover {
  color: var(--brand);
  background: var(--brand-muted);
}

.settings-font-preview {
  background: var(--bg-elevated);
  border: 1px solid var(--border-muted);
  border-radius: 8px;
  padding: 16px;
  margin-top: 16px;
}

.settings-font-preview-sans {
  font-family: var(--font-sans);
  font-size: 14px;
  color: var(--fg-primary);
  line-height: 1.5;
  margin-bottom: 8px;
}

.settings-font-preview-mono {
  font-family: var(--font-mono);
  font-size: 13px;
  color: var(--fg-secondary);
  line-height: 1.5;
}
``````

---

## 10. 导入导出模态框

### 10.1 导入模态框

```
┌──────────────────────────────────────────────────────────────┐
│  Import                                               [✕]   │ 48px
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌── Drop files here or click to browse ──────────────────┐ │
│  │                                                          │ │ 120px
│  │            📁  Drop file here                            │ │
│  │            or click to browse                            │ │
│  │                                                          │ │
│  │   Supports: Postman, OpenAPI, cURL, HAR, Insomnia       │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                               │
│  ── or paste from clipboard ──                               │
│                                                               │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ curl -X GET https://api.example.com/users \             │ │ 80px
│  │   -H "Authorization: Bearer token123"                    │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                               │
│  ── or import from URL ──                                    │
│                                                               │
│  [https://api.example.com/openapi.json      ] [Fetch]        │
│                                                               │
├──────────────────────────────────────────────────────────────┤
│  Detected: OpenAPI 3.0  |  5 endpoints  |  2 schemas        │ 32px
│                                                               │
│  Import to:  [◉ New Collection] [○ Merge to existing ▼]    │
│                                                               │
│                                        [Cancel] [Import]     │ 48px
└──────────────────────────────────────────────────────────────┘
```

``````css
.import-modal-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: var(--z-modal);
  display: flex;
  align-items: center;
  justify-content: center;
}

.import-modal {
  width: 560px;
  max-height: 560px;
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow: var(--shadow-xl);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: fadeInUp 180ms var(--ease-smooth);
}

.import-header {
  display: flex;
  align-items: center;
  height: 48px;
  padding: 0 20px;
  border-bottom: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.import-body {
  flex: 1;
  overflow-y: auto;
  padding: 20px;
}

.import-drop-zone {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 120px;
  border: 2px dashed var(--border-default);
  border-radius: 8px;
  gap: 8px;
  cursor: pointer;
  transition: all 180ms;
  margin-bottom: 16px;
}

.import-drop-zone:hover {
  border-color: var(--brand);
  background: var(--brand-muted);
}

.import-drop-zone.drag-over {
  border-color: var(--brand);
  background: var(--brand-muted);
  box-shadow: var(--glow-brand);
}

.import-drop-icon {
  width: 32px;
  height: 32px;
  color: var(--fg-tertiary);
}

.import-drop-text {
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--fg-primary);
}

.import-drop-hint {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
}

.import-divider {
  display: flex;
  align-items: center;
  gap: 12px;
  margin: 16px 0;
}

.import-divider-line {
  flex: 1;
  height: 1px;
  background: var(--border-muted);
}

.import-divider-text {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  white-space: nowrap;
}

.import-paste-area {
  width: 100%;
  min-height: 80px;
  padding: 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  resize: vertical;
  line-height: 18px;
}

.import-paste-area:focus {
  border-color: var(--border-focus);
}

.import-url-row {
  display: flex;
  gap: 8px;
  margin-top: 16px;
}

.import-url-input {
  flex: 1;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
}

.import-url-input:focus {
  border-color: var(--border-focus);
}

.import-fetch-btn {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--fg-primary);
  cursor: pointer;
  transition: all 100ms;
  flex-shrink: 0;
}

.import-fetch-btn:hover {
  border-color: var(--brand);
  color: var(--brand);
}

.import-detected {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 16px;
  background: var(--brand-muted);
  border-radius: 6px;
  margin-bottom: 12px;
}

.import-detected-badge {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--brand);
  background: rgba(108, 92, 231, 0.2);
  padding: 2px 8px;
  border-radius: 4px;
}

.import-detected-info {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-secondary);
}

.import-target-row {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 8px;
}

.import-target-label {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-secondary);
}

.import-radio {
  display: flex;
  align-items: center;
  gap: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-primary);
  cursor: pointer;
}

.import-footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
  height: 48px;
  padding: 0 20px;
  border-top: 1px solid var(--border-muted);
  flex-shrink: 0;
}
``````

### 10.2 导出模态框

```
┌──────────────────────────────────────────────────────────┐
│  Export Collection                                [✕]    │ 48px
├──────────────────────────────────────────────────────────┤
│                                                           │
│  Collection: My API                                      │
│  Format: [◉ Postman v2.1] [○ OpenAPI 3.0] [○ cURL]    │
│                                                           │
│  Options:                                                │
│  [✓] Include environment variables                       │
│  [✓] Include Pre/Post scripts                            │
│  [ ] Encrypt with password                               │
│                                                           │
│  ┌── Preview ────────────────────────────────────────┐   │
│  │  {                                                  │   │ 200px
│  │    "info": {                                        │   │
│  │      "name": "My API",                              │   │
│  │      "schema": "https://schema.getpostman.com/..."  │   │
│  │    },                                               │   │
│  │    ...                                              │   │
│  │  }                                                  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                           │
│                          [Copy to Clipboard] [Download]   │ 48px
└──────────────────────────────────────────────────────────┘
```

``````css
.export-modal {
  width: 560px;
  max-height: 520px;
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow: var(--shadow-xl);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: fadeInUp 180ms var(--ease-smooth);
}

.export-format-row {
  display: flex;
  gap: 8px;
  margin-bottom: 16px;
}

.export-format-btn {
  height: 32px;
  padding: 0 12px;
  border-radius: 6px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 100ms;
}

.export-format-btn.active {
  background: var(--brand-muted);
  border-color: var(--brand);
  color: var(--brand);
}

.export-format-btn:hover:not(.active) {
  border-color: var(--border-default);
  color: var(--fg-primary);
}

.export-option-row {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 28px;
  margin-bottom: 4px;
}

.export-option-checkbox {
  width: 14px;
  height: 14px;
  border-radius: 3px;
  border: 1.5px solid var(--border-default);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: all 50ms;
}

.export-option-checkbox.checked {
  background: var(--brand);
  border-color: var(--brand);
}

.export-option-label {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-primary);
}

.export-preview {
  flex: 1;
  min-height: 200px;
  max-height: 240px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  overflow: auto;
  margin-top: 12px;
  font-family: var(--font-mono);
  font-size: 12px;
  line-height: 18px;
  color: var(--fg-primary);
  padding: 12px;
}

.export-footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
  height: 48px;
  padding: 0 20px;
  border-top: 1px solid var(--border-muted);
  flex-shrink: 0;
}

.export-copy-btn {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: transparent;
  border: 1px solid var(--border-default);
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: var(--fg-primary);
  cursor: pointer;
  transition: all 100ms;
}

.export-copy-btn:hover {
  background: var(--bg-hover);
}

.export-download-btn {
  height: 32px;
  padding: 0 16px;
  border-radius: 6px;
  background: var(--brand);
  border: none;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 500;
  color: #FFFFFF;
  cursor: pointer;
  transition: all 100ms;
}

.export-download-btn:hover {
  background: var(--brand-hover);
  box-shadow: 0 0 16px rgba(108, 92, 231, 0.25);
}
``````

---

## 11. Vault 解锁模态框

### 11.1 布局

```
┌──────────────────────────────────────────────────┐
│                                                   │
│                   🔒                              │
│                                                   │
│              Vault is Locked                      │
│     Enter your master password to unlock          │
│                                                   │
│  ┌──────────────────────────────────────────────┐│
│  │  ••••••••••••                                 ││ 36px
│  └──────────────────────────────────────────────┘│
│                                                   │
│  [✓] Remember for this session                    │
│                                                   │
│  [        Unlock Vault        ]                   │ 36px
│                                                   │
│  Forgot password? Reset vault data                │
│                                                   │
└──────────────────────────────────────────────────┘
```

### 11.2 模态容器

``````css
.vault-unlock-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(8px);
  z-index: var(--z-modal);
  display: flex;
  align-items: center;
  justify-content: center;
}

.vault-unlock-modal {
  width: 380px;
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow: var(--shadow-xl);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32px 28px;
  animation: fadeInUp 180ms var(--ease-smooth);
}
``````

### 11.3 图标与标题

``````css
.vault-unlock-icon {
  width: 48px;
  height: 48px;
  color: var(--brand);
  margin-bottom: 16px;
}

.vault-unlock-title {
  font-family: var(--font-sans);
  font-size: 16px;
  font-weight: 600;
  color: var(--fg-primary);
  margin-bottom: 4px;
}

.vault-unlock-desc {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-tertiary);
  text-align: center;
  margin-bottom: 20px;
}
``````

### 11.4 密码输入

``````css
.vault-unlock-input-wrapper {
  width: 100%;
  position: relative;
  margin-bottom: 12px;
}

.vault-unlock-input {
  width: 100%;
  height: 36px;
  padding: 0 36px 0 12px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 13px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.vault-unlock-input:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.vault-unlock-input.error {
  border-color: var(--accent-danger);
  box-shadow: 0 0 0 3px rgba(255, 71, 87, 0.1);
}

.vault-unlock-toggle-visibility {
  position: absolute;
  right: 8px;
  top: 50%;
  transform: translateY(-50%);
  width: 20px;
  height: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--fg-tertiary);
  cursor: pointer;
  border-radius: 3px;
  transition: color 50ms;
}

.vault-unlock-toggle-visibility:hover {
  color: var(--fg-primary);
}

.vault-unlock-error {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--accent-danger);
  margin-top: 4px;
  margin-bottom: 8px;
  width: 100%;
}
``````

### 11.5 操作区

``````css
.vault-unlock-remember {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  margin-bottom: 16px;
}

.vault-unlock-remember-label {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-secondary);
  cursor: pointer;
}

.vault-unlock-btn {
  width: 100%;
  height: 36px;
  border-radius: 6px;
  background: var(--brand);
  border: none;
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 600;
  color: #FFFFFF;
  cursor: pointer;
  transition: all 100ms;
  margin-bottom: 12px;
}

.vault-unlock-btn:hover {
  background: var(--brand-hover);
  box-shadow: 0 0 16px rgba(108, 92, 231, 0.25);
}

.vault-unlock-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.vault-unlock-btn:active {
  transform: scale(0.98);
}

.vault-unlock-forgot {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  text-align: center;
}

.vault-unlock-forgot-link {
  color: var(--accent-danger);
  cursor: pointer;
  text-decoration: none;
}

.vault-unlock-forgot-link:hover {
  text-decoration: underline;
}
``````

### 11.6 状态变体

| 状态 | 密码输入框 | 解锁按钮 | 说明 |
|------|-----------|----------|------|
| 默认 | 灰色边框 | 品牌色 | 等待输入 |
| 聚焦 | 蓝色边框+外发光 | 品牌色 | 输入中 |
| 密码错误 | 红色边框+外发光+错误提示 | 品牌色 | "Incorrect password" |
| 提交中 | 灰色 | 品牌色+spinner | 解锁验证中 |
| 空密码 | — | disabled(0.4) | 按钮不可点击 |

---

## 附录 A：全局关键帧动画

``````css
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}

@keyframes fadeInUp {
  from { transform: translateY(8px); opacity: 0; }
  to   { transform: translateY(0); opacity: 1; }
}

@keyframes slideInRight {
  from { transform: translateX(100%); opacity: 0; }
  to   { transform: translateX(0); opacity: 1; }
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to   { transform: translateY(0); }
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to   { transform: translateY(100%); }
}

@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 12px rgba(108, 92, 231, 0.3); }
  50%       { box-shadow: 0 0 24px rgba(108, 92, 231, 0.6); }
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

@keyframes skeletonPulse {
  0%, 100% { opacity: 0.4; }
  50% { opacity: 0.8; }
}

@keyframes cmdSlideIn {
  from { opacity: 0; transform: translateY(-8px) scale(0.98); }
  to   { opacity: 1; transform: translateY(0) scale(1); }
}
``````

---

## 附录 B：组件状态矩阵

| 组件 | default | hover | active/pressed | focus | disabled | loading | error | empty |
|------|---------|-------|----------------|-------|----------|---------|-------|-------|
| 发送按钮 | --brand | --brand-hover + glow | scale(0.98) | — | opacity:0.4 | pulseGlow | --accent-danger | — |
| 方法选择器 | 方法色底 | brightness(1.15) | — | — | — | — | — | — |
| URL 输入框 | --bg-input | — | — | 蓝色边框+外发光 | — | — | — | placeholder |
| Tab 项 | --fg-secondary | --bg-hover | — | — | — | — | — | — |
| KV 行 | 透明底 | --bg-hover | — | — | opacity:0.4 | — | — | 提示文字 |
| 集合树节点 | 透明底 | --bg-hover | --brand-muted | — | — | — | — | — |
| 命令面板项 | 透明底 | --bg-hover | --brand-muted | — | — | — | — | 空搜索图标 |
| Toggle | --bg-active | — | — | — | opacity:0.4 | — | — | — |
| 环境药丸 | 方法色底 | brightness(1.2) | — | — | — | — | — | — |
| 响应状态药丸 | 语义色底 | — | — | — | — | spinner | — | 空状态图标+文字 |
| Console 行 | 透明底 | --bg-hover | — | — | — | spinner | 红色文字 | 空状态图标 |

---

## 附录 C：间距速查

| 场景 | 间距值 |
|------|--------|
| URL 栏内元素间距 | 4px |
| Tab 栏内 Tab 间距 | 0 (仅 padding) |
| 请求 Tab 间距 | 0 |
| KV 编辑器单元格间距 | 8px (padding) |
| 侧边栏节点层级缩进 | 16px/级 |
| 卡片内 padding | 16-20px |
| 面板间 gap | 8-12px |
| 抽屉内容 padding | 20px |
| 设置项间距 | 12px |
| 搜索结果行高 | 36px |
| 集合树节点行高 | 28px |
| KV 编辑器行高 | 32px |
| Console 行高 | 24px |

---

*文档版本: v2.0*
*创建时间: 2026-04-15*
*最后更新: 2026-04-16*
*基于: 02-UI设计.md v2.0 设计系统*

---

## 附录 D：侧边栏视觉规范

### D.1 侧边栏整体

```css
.sidebar {
  width: var(--sidebar-width);
  min-width: var(--sidebar-collapsed);
  background: var(--bg-surface);
  border-right: 1px solid var(--border-muted);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  transition: width 180ms var(--ease-smooth);
}

.sidebar.collapsed {
  width: var(--sidebar-collapsed);
}

.sidebar-section {
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.sidebar-section + .sidebar-section {
  border-top: 1px solid var(--border-muted);
}
```

### D.2 搜索栏

```css
.sidebar-search {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 40px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
}

.sidebar-search-input {
  flex: 1;
  height: 28px;
  padding: 0 8px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.sidebar-search-input:focus {
  border-color: var(--border-focus);
}

.sidebar-search-input::placeholder {
  color: var(--fg-tertiary);
}

.sidebar-add-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.sidebar-add-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}
```

### D.3 区域标题

```css
.sidebar-section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 28px;
  padding: 0 12px;
  cursor: pointer;
  user-select: none;
}

.sidebar-section-title {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.sidebar-section-toggle {
  width: 16px;
  height: 16px;
  color: var(--fg-tertiary);
  transition: transform 100ms var(--ease-default);
}

.sidebar-section.collapsed .sidebar-section-toggle {
  transform: rotate(-90deg);
}
```

### D.4 集合树节点

```css
.tree-node {
  display: flex;
  align-items: center;
  height: 28px;
  padding-right: 8px;
  border-radius: 4px;
  cursor: pointer;
  transition: background 50ms;
}

.tree-node:hover { background: var(--bg-hover); }
.tree-node.active { background: var(--brand-muted); }

.tree-node-folder {
  padding-left: calc(8px + var(--depth, 0) * 16px);
  gap: 4px;
}

.tree-node-request {
  padding-left: calc(8px + var(--depth, 0) * 16px + 16px);
  gap: 6px;
}

.tree-node-icon {
  width: 14px;
  height: 14px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.tree-node-label {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.tree-node-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  padding: 1px 6px;
  border-radius: 9999px;
  flex-shrink: 0;
}

.tree-node-method.get    { color: var(--method-get);    background: rgba(0, 214, 143, 0.12); }
.tree-node-method.post   { color: var(--method-post);   background: rgba(255, 184, 0, 0.12); }
.tree-node-method.put    { color: var(--method-put);    background: rgba(91, 141, 239, 0.12); }
.tree-node-method.patch  { color: var(--method-patch);  background: rgba(191, 90, 242, 0.12); }
.tree-node-method.delete { color: var(--method-delete); background: rgba(255, 71, 87, 0.12); }
```

### D.5 环境列表

```css
.env-item {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 28px;
  padding: 0 12px;
  border-radius: 4px;
  cursor: pointer;
  transition: background 50ms;
}

.env-item:hover { background: var(--bg-hover); }
.env-item.active { background: var(--brand-muted); }

.env-item-dot {
  width: 6px;
  height: 6px;
  border-radius: 9999px;
  flex-shrink: 0;
}

.env-item.active .env-item-dot { background: var(--brand); }
.env-item:not(.active) .env-item-dot { background: var(--fg-tertiary); }

.env-item-name {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-primary);
}
```

### D.6 历史列表

```css
.history-group-title {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  padding: 6px 12px 2px;
}

.history-item {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 12px;
  border-radius: 4px;
  cursor: pointer;
  transition: background 50ms;
}

.history-item:hover { background: var(--bg-hover); }

.history-item-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  min-width: 34px;
}

.history-item-url {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--fg-secondary);
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.history-item-time {
  font-family: var(--font-sans);
  font-size: 10px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}
```

---

## 附录 E：命令面板视觉规范

```css
.command-palette-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: var(--z-command-palette);
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding-top: 20vh;
}

.command-palette {
  width: 560px;
  max-height: 420px;
  background: var(--glass-bg);
  backdrop-filter: blur(20px);
  border: 1px solid var(--glass-border);
  border-radius: 12px;
  box-shadow: var(--shadow-xl), 0 0 60px rgba(108, 92, 231, 0.08);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  animation: fadeInUp 100ms var(--ease-smooth);
}

.command-palette-search {
  display: flex;
  align-items: center;
  gap: 10px;
  height: 48px;
  padding: 0 16px;
  border-bottom: 1px solid var(--border-muted);
}

.command-palette-search-icon {
  width: 18px;
  height: 18px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.command-palette-search-input {
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-sans);
  font-size: 14px;
  color: var(--fg-primary);
}

.command-palette-search-input::placeholder {
  color: var(--fg-tertiary);
}

.command-palette-results {
  flex: 1;
  overflow-y: auto;
  padding: 8px;
}

.command-palette-section-title {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 6px 8px 4px;
}

.command-palette-item {
  display: flex;
  align-items: center;
  gap: 10px;
  height: 32px;
  padding: 0 10px;
  border-radius: 6px;
  cursor: pointer;
  transition: background 50ms;
}

.command-palette-item:hover,
.command-palette-item.focused {
  background: var(--bg-hover);
}

.command-palette-item.focused {
  background: var(--brand-muted);
}

.command-palette-item-icon {
  width: 16px;
  height: 16px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
}

.command-palette-item-label {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-primary);
  flex: 1;
}

.command-palette-item-shortcut {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  color: var(--fg-tertiary);
  padding: 2px 6px;
  background: var(--bg-active);
  border-radius: 4px;
}
```

---

## 附录 F：Console 面板视觉规范

```css
.console-panel {
  position: fixed;
  bottom: 0;
  left: var(--sidebar-width);
  right: 0;
  height: 240px;
  background: var(--bg-surface);
  border-top: 1px solid var(--border-default);
  z-index: var(--z-sticky);
  display: flex;
  flex-direction: column;
  animation: slideUp 180ms var(--ease-smooth);
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.console-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 32px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
  background: var(--bg-elevated);
}

.console-title {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.console-actions {
  display: flex;
  gap: 4px;
}

.console-action-btn {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.console-action-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.console-body {
  flex: 1;
  overflow-y: auto;
  padding: 4px 0;
  font-family: var(--font-mono);
  font-size: 12px;
  line-height: 20px;
}

.console-line {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  padding: 0 12px;
  min-height: 20px;
}

.console-line:hover {
  background: var(--bg-hover);
}

.console-line-timestamp {
  font-size: 10px;
  color: var(--fg-tertiary);
  flex-shrink: 0;
  padding-top: 1px;
}

.console-line-direction {
  font-weight: 600;
  flex-shrink: 0;
}

.console-line-direction.out {
  color: var(--accent-info);
}

.console-line-direction.in {
  color: var(--accent-success);
}

.console-line-content {
  color: var(--fg-secondary);
  flex: 1;
  word-break: break-all;
}

.console-line.error .console-line-content {
  color: var(--accent-danger);
}

.console-line.warn .console-line-content {
  color: var(--accent-warning);
}
```

---

## 附录 G：Settings 模态框视觉规范

```css
.settings-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  z-index: var(--z-modal);
  display: flex;
  align-items: center;
  justify-content: center;
}

.settings-modal {
  width: 640px;
  max-height: 520px;
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow: var(--shadow-xl);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  animation: fadeInUp 100ms var(--ease-smooth);
}

.settings-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 48px;
  padding: 0 20px;
  border-bottom: 1px solid var(--border-muted);
}

.settings-title {
  font-family: var(--font-sans);
  font-size: 16px;
  font-weight: 600;
  color: var(--fg-primary);
}

.settings-close {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.settings-close:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.settings-body {
  flex: 1;
  display: flex;
  overflow: hidden;
}

.settings-nav {
  width: 180px;
  border-right: 1px solid var(--border-muted);
  padding: 8px;
}

.settings-nav-item {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 32px;
  padding: 0 10px;
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.settings-nav-item:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.settings-nav-item.active {
  background: var(--brand-muted);
  color: var(--brand);
}

.settings-content {
  flex: 1;
  padding: 20px;
  overflow-y: auto;
}

.settings-group {
  margin-bottom: 20px;
}

.settings-group-title {
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 600;
  color: var(--fg-primary);
  margin-bottom: 12px;
}

.settings-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 36px;
  padding: 0 4px;
}

.settings-row + .settings-row {
  border-top: 1px solid var(--border-muted);
}

.settings-row-label {
  font-family: var(--font-sans);
  font-size: 13px;
  color: var(--fg-primary);
}

.settings-row-desc {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  margin-top: 2px;
}
```
