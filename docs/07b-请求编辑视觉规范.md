# API Client 请求编辑视觉规范

> 本文档拆分自原 07-核心页面视觉规范.md，涵盖请求编辑页（Request Editor Page）视觉规范。其他页面视觉规范见 07a/07c。
> CSS 变量命名规范及设计 Token 速查见 07a-首页视觉规范.md。

## 2. 请求编辑页（Request Editor Page）

> 这是应用最核心、最复杂的页面，规范最为详细。

### 2.1 布局总览

```
┌──────────┬──────────────────────────────────────────────────────────────────────────────┐
│          │ [≡] [←][→]  [ GET ▼ ]  [ https://api.example.com/{{base}}/users  ]  [ ▶ ]  │ 44px URL栏
│          │              [ Dev ▼ ]  [⌘K]                                   [ ⚙ ▼ ]  │
│  Side    ├──────────────────────────────────────────────────────────────────────────────┤
│  bar     │ [×] [GET /users] [POST /orders] [+]                              [⋮][⌘\][⛶]│ 36px Tab Bar
│          ├──────────────────────────────────────────────────────────────────────────────┤
│  220px   │ [ Params ] [ Headers ] [ Body ] [ Auth ] [ Scripts ]                         │ 36px 请求Tab
│          ├──────────────────────────────────────────────────────────────────────────────┤
│          │                                                                              │
│          │                      请求编辑内容区                                             │
│          │                      （高度可变，由分屏条分割）                                  │
│          ├─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤ 4px 分割条
│          │ [200 OK] [245ms] [1.2KB]                     [⌘\ 分屏] [⛶ 全屏]             │ 32px 响应状态
│          ├──────────────────────────────────────────────────────────────────────────────┤
│          │ [ Body ] [ Headers ] [ Cookies ] [ Tests ]                                  │ 36px 响应Tab
│          ├──────────────────────────────────────────────────────────────────────────────┤
│          │                                                                              │
│          │                      响应查看内容区                                             │
│          │                                                                              │
│          └──────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 URL 栏

```css
.url-bar {
  display: flex;
  align-items: center;
  height: 44px;
  padding: 0 8px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  gap: 4px;
}

.url-bar-toggle {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms var(--ease-default);
  flex-shrink: 0;
}

.url-bar-toggle:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.url-bar-toggle:active {
  background: var(--bg-active);
}

.url-bar-nav {
  display: flex;
  gap: 2px;
}

.url-bar-nav-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms var(--ease-default);
}

.url-bar-nav-btn:hover:not(:disabled) {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.url-bar-nav-btn:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}

.url-bar-separator {
  width: 1px;
  height: 20px;
  background: var(--border-muted);
  margin: 0 4px;
  flex-shrink: 0;
}

.method-selector {
  position: relative;
  flex-shrink: 0;
}

.method-selector-trigger {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 32px;
  padding: 0 10px 0 12px;
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 600;
  line-height: 16px;
  cursor: pointer;
  transition: all 100ms var(--ease-default);
  user-select: none;
}

.method-selector-trigger.get    { color: var(--method-get);    background: rgba(0, 214, 143, 0.12); }
.method-selector-trigger.post   { color: var(--method-post);   background: rgba(255, 184, 0, 0.12); }
.method-selector-trigger.put    { color: var(--method-put);    background: rgba(91, 141, 239, 0.12); }
.method-selector-trigger.patch  { color: var(--method-patch);  background: rgba(191, 90, 242, 0.12); }
.method-selector-trigger.delete { color: var(--method-delete); background: rgba(255, 71, 87, 0.12); }

.method-selector-trigger:hover {
  filter: brightness(1.15);
}

.method-selector-trigger .chevron {
  width: 12px;
  height: 12px;
  opacity: 0.5;
  transition: transform 100ms var(--ease-default);
}

.method-selector.open .chevron {
  transform: rotate(180deg);
}

.method-dropdown {
  position: absolute;
  top: calc(100% + 4px);
  left: 0;
  width: 160px;
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  border-radius: 8px;
  padding: 4px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  z-index: var(--z-dropdown);
  animation: fadeInUp 100ms var(--ease-smooth);
}

.method-dropdown-item {
  display: flex;
  align-items: center;
  height: 30px;
  padding: 0 10px;
  border-radius: 4px;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: background 50ms;
}

.method-dropdown-item:hover {
  background: var(--bg-hover);
}

.method-dropdown-item.active {
  background: var(--brand-muted);
}

.url-input-wrapper {
  flex: 1;
  height: 32px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  transition: border-color 100ms var(--ease-default);
}

.url-input-wrapper:focus-within {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.url-input {
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
}

.url-input::placeholder {
  color: var(--fg-tertiary);
}

.url-input .variable {
  color: var(--brand);
  background: var(--brand-muted);
  border-radius: 2px;
  padding: 0 2px;
  cursor: pointer;
}

.url-input .variable:hover {
  background: rgba(108, 92, 231, 0.25);
}

.send-btn {
  width: 40px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  background: var(--brand);
  color: #FFFFFF;
  cursor: pointer;
  flex-shrink: 0;
  transition: all 100ms var(--ease-default);
}

.send-btn:hover {
  background: var(--brand-hover);
  box-shadow: 0 0 20px rgba(108, 92, 231, 0.3);
  transform: scale(1.02);
}

.send-btn:active {
  transform: scale(0.98);
  background: var(--brand);
  box-shadow: none;
}

.send-btn.sending {
  animation: pulseGlow 1.5s ease-in-out infinite;
  pointer-events: none;
}

.send-btn.completed {
  background: var(--accent-success);
  transition: background 100ms;
}

.send-btn.completed::after {
  content: '\2713';
  font-size: 12px;
  font-weight: 700;
}

.send-btn.completed.returning {
  background: var(--brand);
  transition: background 180ms var(--ease-default);
}

.send-btn.error {
  background: var(--accent-danger);
}

.send-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
}

@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 12px rgba(108, 92, 231, 0.3); }
  50%       { box-shadow: 0 0 24px rgba(108, 92, 231, 0.6); }
}
```

### 2.3 URL 栏第二行

```css
.url-bar-row2 {
  display: flex;
  align-items: center;
  height: 28px;
  padding: 0 8px;
  gap: 8px;
}

.env-selector {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 22px;
  padding: 0 10px;
  border-radius: 9999px;
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  line-height: 14px;
  cursor: pointer;
  transition: all 100ms var(--ease-default);
  user-select: none;
}

.env-selector.development {
  color: var(--accent-success);
  background: rgba(0, 214, 143, 0.1);
  border: 1px solid rgba(0, 214, 143, 0.2);
}

.env-selector.staging {
  color: var(--accent-warning);
  background: rgba(255, 184, 0, 0.1);
  border: 1px solid rgba(255, 184, 0, 0.2);
}

.env-selector.production {
  color: var(--accent-danger);
  background: rgba(255, 71, 87, 0.1);
  border: 1px solid rgba(255, 71, 87, 0.2);
}

.env-selector:hover {
  filter: brightness(1.2);
}

.env-selector-dot {
  width: 6px;
  height: 6px;
  border-radius: 9999px;
  background: currentColor;
}

.url-bar-cmd {
  width: 28px;
  height: 22px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  font-size: 10px;
  font-weight: 500;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.url-bar-cmd:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.url-bar-menu {
  margin-left: auto;
  width: 28px;
  height: 22px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.url-bar-menu:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}
```

### 2.4 Tab 栏

```css
.tab-bar {
  display: flex;
  align-items: center;
  height: 36px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  padding: 0 4px;
  overflow-x: auto;
  overflow-y: hidden;
  scrollbar-width: none;
}

.tab-bar::-webkit-scrollbar { display: none; }

.tab-item {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 12px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-secondary);
  line-height: 16px;
  cursor: pointer;
  white-space: nowrap;
  max-width: 180px;
  min-width: 80px;
  transition: all 50ms var(--ease-default);
  position: relative;
  flex-shrink: 0;
}

.tab-item:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.tab-item.active {
  color: var(--fg-primary);
  background: var(--bg-hover);
}

.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 8px;
  right: 8px;
  height: 2px;
  background: var(--brand);
  border-radius: 1px;
}

.tab-item-method {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
}

.tab-item-method.get    { color: var(--method-get); }
.tab-item-method.post   { color: var(--method-post); }
.tab-item-method.put    { color: var(--method-put); }
.tab-item-method.patch  { color: var(--method-patch); }
.tab-item-method.delete { color: var(--method-delete); }

.tab-item-label {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.tab-item-close {
  width: 16px;
  height: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  opacity: 0;
  color: var(--fg-tertiary);
  transition: all 50ms;
  flex-shrink: 0;
}

.tab-item:hover .tab-item-close { opacity: 1; }

.tab-item-close:hover {
  background: var(--bg-active);
  color: var(--fg-primary);
}

.tab-item-dirty {
  width: 6px;
  height: 6px;
  border-radius: 9999px;
  background: var(--accent-warning);
  flex-shrink: 0;
}

.tab-item-dirty + .tab-item-close { display: none; }
.tab-item:hover .tab-item-dirty { display: none; }
.tab-item:hover .tab-item-close { display: flex; }

.tab-bar-add {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
  flex-shrink: 0;
}

.tab-bar-add:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.tab-bar-tools {
  margin-left: auto;
  display: flex;
  gap: 2px;
  flex-shrink: 0;
  padding-left: 8px;
}

.tab-bar-tool-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.tab-bar-tool-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.tab-scroll-btn {
  width: 20px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--fg-tertiary);
  cursor: pointer;
  flex-shrink: 0;
}

.tab-scroll-btn:hover { color: var(--fg-secondary); }
```

### 2.5 请求 Tab 栏

```css
.request-tabs {
  display: flex;
  align-items: center;
  height: 36px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  padding: 0 12px;
  gap: 0;
  position: relative;
}

.request-tab {
  height: 36px;
  padding: 0 14px;
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  line-height: 16px;
  cursor: pointer;
  white-space: nowrap;
  position: relative;
  transition: color 50ms;
}

.request-tab:hover { color: var(--fg-primary); }
.request-tab.active { color: var(--fg-primary); }

.request-tab-indicator {
  position: absolute;
  bottom: 0;
  height: 2px;
  background: var(--brand);
  border-radius: 1px;
  transition: left 180ms var(--ease-smooth),
              width 180ms var(--ease-smooth);
}

.request-tab-badge {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  min-width: 16px;
  height: 16px;
  padding: 0 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 9999px;
  color: var(--fg-tertiary);
  background: var(--bg-active);
}

.request-tab.active .request-tab-badge {
  color: var(--brand);
  background: var(--brand-muted);
}
```

### 2.6 Key-Value 编辑器

```css
.kv-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
  overflow: hidden;
}

.kv-editor-header {
  display: grid;
  grid-template-columns: 20px 200px 1fr 180px 28px;
  height: 28px;
  padding: 0 12px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--fg-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.kv-editor-body {
  flex: 1;
  overflow-y: auto;
  padding: 0 12px;
}

.kv-row {
  display: grid;
  grid-template-columns: 20px 200px 1fr 180px 28px;
  height: 32px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.kv-row:hover { background: var(--bg-hover); }

.kv-row-checkbox {
  width: 14px;
  height: 14px;
  border-radius: 4px;
  border: 1.5px solid var(--border-default);
  background: transparent;
  cursor: pointer;
  transition: all 50ms;
  display: flex;
  align-items: center;
  justify-content: center;
}

.kv-row-checkbox.checked {
  background: var(--brand);
  border-color: var(--brand);
}

.kv-row-checkbox.checked::after {
  content: '\2713';
  font-size: 9px;
  color: #FFFFFF;
  font-weight: 700;
}

.kv-row.disabled .kv-row-checkbox { opacity: 0.35; }

.kv-row-key {
  height: 100%;
  display: flex;
  align-items: center;
  padding: 0 8px;
}

.kv-row-key input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
}

.kv-row-key input::placeholder { color: var(--fg-tertiary); }

.kv-row-value {
  height: 100%;
  display: flex;
  align-items: center;
  padding: 0 8px;
}

.kv-row-value input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-primary);
  line-height: 16px;
}

.kv-row-value .variable {
  color: var(--brand);
  background: var(--brand-muted);
  border-radius: 2px;
  padding: 0 2px;
}

.kv-row-desc {
  height: 100%;
  display: flex;
  align-items: center;
  padding: 0 8px;
}

.kv-row-desc input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-secondary);
  line-height: 16px;
}

.kv-row-desc input::placeholder { color: var(--fg-tertiary); }

.kv-row-delete {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  opacity: 0;
  transition: all 50ms;
}

.kv-row:hover .kv-row-delete { opacity: 1; }

.kv-row-delete:hover {
  background: rgba(255, 71, 87, 0.12);
  color: var(--accent-danger);
}

.kv-row.disabled { opacity: 0.4; }
.kv-row.disabled input { text-decoration: line-through; }

.kv-editor-add {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 32px;
  padding: 0 12px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.kv-editor-add:hover {
  color: var(--brand);
  background: var(--brand-muted);
}

.kv-editor-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 8px;
  color: var(--fg-tertiary);
  font-size: 12px;
}
```

### 2.7 Body 编辑器

```css
.body-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.body-type-row {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 36px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
}

.body-type-radio {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 24px;
  padding: 0 10px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.body-type-radio:hover {
  background: var(--bg-hover);
  color: var(--fg-primary);
}

.body-type-radio.active {
  color: var(--brand);
  background: var(--brand-muted);
}

.body-type-dot {
  width: 10px;
  height: 10px;
  border-radius: 9999px;
  border: 1.5px solid var(--border-default);
  transition: all 100ms;
}

.body-type-radio.active .body-type-dot {
  border-color: var(--brand);
  background: var(--brand);
  box-shadow: inset 0 0 0 2px var(--bg-surface);
}

.body-type-separator {
  width: 1px;
  height: 16px;
  background: var(--border-muted);
}

.raw-type-selector {
  display: flex;
  align-items: center;
  height: 24px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  gap: 4px;
  transition: all 50ms;
}

.raw-type-selector:hover { background: var(--bg-hover); }

.body-code-editor {
  flex: 1;
  overflow: hidden;
  position: relative;
}

.body-code-editor .monaco-editor { padding: 8px 0; }

.form-data-row {
  display: grid;
  grid-template-columns: 20px 200px 80px 1fr 28px;
  height: 32px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.form-data-row:hover { background: var(--bg-hover); }

.form-data-type-select {
  height: 24px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  background: var(--bg-active);
  cursor: pointer;
  border: none;
  outline: none;
}

.binary-upload-zone {
  width: 280px;
  height: 160px;
  border: 1.5px dashed var(--border-default);
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  cursor: pointer;
  transition: all 180ms var(--ease-default);
}

.binary-upload-zone:hover {
  border-color: var(--brand);
  background: var(--brand-muted);
}

.binary-upload-zone.dragover {
  border-color: var(--brand);
  background: rgba(108, 92, 231, 0.1);
  box-shadow: 0 0 20px rgba(108, 92, 231, 0.15);
}

.graphql-editor {
  display: grid;
  grid-template-columns: 1fr 1fr;
  height: 100%;
  gap: 0;
}

.graphql-editor-query {
  border-right: 1px solid var(--border-muted);
}
```

### 2.8 Auth 面板

``````css
.auth-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 16px 12px;
  gap: 12px;
}

.auth-type-select {
  width: 220px;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-primary);
  cursor: pointer;
  transition: border-color 100ms;
}

.auth-type-select:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.auth-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.auth-field-label {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
  color: var(--fg-secondary);
  line-height: 16px;
}

.auth-field-input {
  width: 100%;
  height: 32px;
  padding: 0 10px;
  background: var(--bg-input);
  border: 1px solid var(--border-muted);
  border-radius: 6px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--fg-primary);
  outline: none;
  transition: border-color 100ms;
}

.auth-field-input:focus {
  border-color: var(--border-focus);
  box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
}

.auth-field-input::placeholder {
  color: var(--fg-tertiary);
  font-family: var(--font-sans);
}

.auth-field-input.password { padding-right: 36px; }

.auth-hint {
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--fg-tertiary);
  line-height: 16px;
  padding: 8px 12px;
  background: var(--bg-elevated);
  border-radius: 6px;
  border-left: 2px solid var(--accent-info);
}
``````

### 2.9 Scripts 面板

``````css
.scripts-editor {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.scripts-tabs {
  display: flex;
  height: 36px;
  border-bottom: 1px solid var(--border-muted);
  padding: 0 12px;
  gap: 0;
  position: relative;
}

.scripts-tab {
  height: 36px;
  padding: 0 14px;
  display: flex;
  align-items: center;
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: color 50ms;
}

.scripts-tab:hover { color: var(--fg-primary); }
.scripts-tab.active { color: var(--fg-primary); }

.scripts-code-area { flex: 1; overflow: hidden; }

.scripts-snippet-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 10px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.scripts-snippet-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}
``````

### 2.10 分割条

``````css
.split-handle {
  height: 4px;
  background: transparent;
  transition: background 50ms;
  cursor: row-resize;
  position: relative;
  flex-shrink: 0;
}

.split-handle::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 32px;
  height: 2px;
  border-radius: 1px;
  background: transparent;
  transition: background 100ms;
}

.split-handle:hover { background: var(--bg-hover); }
.split-handle:hover::after { background: var(--fg-tertiary); }
.split-handle:active { background: var(--brand); }
.split-handle:active::after { background: #FFFFFF; }
.split-handle.dragging { background: var(--brand); }
``````

### 2.11 响应状态条

``````css
.response-bar {
  display: flex;
  align-items: center;
  height: 32px;
  padding: 0 12px;
  gap: 8px;
  background: var(--bg-surface);
  border-top: 1px solid var(--border-muted);
  border-bottom: 1px solid var(--border-muted);
}

.response-status-pill {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 20px;
  padding: 0 8px;
  border-radius: 9999px;
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 600;
  line-height: 14px;
}

.response-status-pill.s2xx { color: var(--accent-success); background: rgba(0, 214, 143, 0.12); }
.response-status-pill.s3xx { color: var(--accent-info);    background: rgba(91, 141, 239, 0.12); }
.response-status-pill.s4xx { color: var(--accent-warning); background: rgba(255, 184, 0, 0.12); }
.response-status-pill.s5xx { color: var(--accent-danger);  background: rgba(255, 71, 87, 0.12); }

.response-meta {
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 400;
  color: var(--fg-tertiary);
  line-height: 14px;
}

.response-bar-tools {
  margin-left: auto;
  display: flex;
  gap: 2px;
}

.response-bar-btn {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  color: var(--fg-tertiary);
  cursor: pointer;
  transition: all 50ms;
}

.response-bar-btn:hover {
  background: var(--bg-hover);
  color: var(--fg-secondary);
}

.response-bar-btn.active {
  color: var(--brand);
  background: var(--brand-muted);
}
``````

### 2.12 响应 Tab 栏

``````css
/* 复用 .request-tabs / .request-tab 样式体系 */
.response-tabs {
  display: flex;
  align-items: center;
  height: 36px;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-muted);
  padding: 0 12px;
  gap: 0;
  position: relative;
}
``````

### 2.13 JSON 查看器

``````css
.json-viewer {
  padding: 8px 12px;
  font-family: var(--font-mono);
  font-size: 12px;
  line-height: 20px;
  overflow: auto;
  height: 100%;
}

.json-key     { color: #5B8DEF; }
.json-string  { color: #00D68F; }
.json-number  { color: #FFB800; }
.json-boolean { color: #BF5AF2; }
.json-null    { color: var(--fg-tertiary); font-style: italic; }
.json-bracket { color: var(--fg-tertiary); }

.json-line {
  padding: 0 0 0 calc(var(--indent) * 16px);
  transition: background 50ms;
  border-radius: 2px;
}

.json-line:hover { background: var(--bg-hover); }

.json-fold {
  cursor: pointer;
  color: var(--fg-tertiary);
  user-select: none;
}

.json-fold:hover { color: var(--fg-secondary); }

.json-preview {
  color: var(--fg-tertiary);
  font-style: italic;
  font-size: 11px;
  margin-left: 8px;
}
``````

### 2.14 HTML/Text 预览

``````css
.response-preview-toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 32px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border-muted);
}

.response-preview-mode {
  display: flex;
  align-items: center;
  gap: 4px;
  height: 24px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 500;
  color: var(--fg-secondary);
  cursor: pointer;
  transition: all 50ms;
}

.response-preview-mode.active {
  color: var(--brand);
  background: var(--brand-muted);
}

.response-preview-mode:hover { background: var(--bg-hover); }
``````

### 2.15 Headers 列表

``````css
.response-header-row {
  display: grid;
  grid-template-columns: 240px 1fr;
  height: 28px;
  padding: 0 12px;
  align-items: center;
  border-bottom: 1px solid var(--border-muted);
  transition: background 50ms;
}

.response-header-row:hover { background: var(--bg-hover); }

.response-header-key {
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 600;
  color: var(--fg-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.response-header-value {
  font-family: var(--font-mono);
  font-size: 12px;
  font-weight: 400;
  color: var(--fg-secondary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
``````

### 2.16 Tests 结果

``````css
.test-result {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 28px;
  padding: 0 8px;
  border-radius: 4px;
  font-family: var(--font-sans);
  font-size: 12px;
  transition: background 50ms;
}

.test-result:hover { background: var(--bg-hover); }

.test-result-icon.pass { color: var(--accent-success); }
.test-result-icon.fail { color: var(--accent-danger); }

.test-summary-pass {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--accent-success);
}

.test-summary-fail {
  font-family: var(--font-sans);
  font-size: 12px;
  font-weight: 600;
  color: var(--accent-danger);
}
``````

### 2.17 响应区空状态

``````css
.response-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  text-align: center;
}

.response-empty-icon {
  width: 48px;
  height: 48px;
  color: var(--fg-tertiary);
  opacity: 0.3;
}

.response-empty-text {
  font-family: var(--font-sans);
  font-size: 13px;
  font-weight: 400;
  color: var(--fg-tertiary);
}

.response-empty-shortcut {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--fg-tertiary);
  padding: 2px 8px;
  background: var(--bg-active);
  border-radius: 4px;
}
``````

### 2.18 响应区加载状态

``````css
.response-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 16px;
}

.response-loading-spinner {
  width: 24px;
  height: 24px;
  border: 2px solid var(--border-default);
  border-top-color: var(--brand);
  border-radius: 9999px;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.response-loading-timer {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--brand);
}
``````

### 2.19 响应区错误状态

``````css
.response-error {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  text-align: center;
  padding: 24px;
}

.response-error-icon {
  width: 40px;
  height: 40px;
  color: var(--accent-danger);
}

.response-error-title {
  font-family: var(--font-sans);
  font-size: 14px;
  font-weight: 500;
  color: var(--fg-primary);
}

.response-error-message {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--accent-danger);
  background: rgba(255, 71, 87, 0.08);
  padding: 8px 12px;
  border-radius: 6px;
  max-width: 480px;
  word-break: break-all;
}

.response-error-hint {
  font-family: var(--font-sans);
  font-size: 12px;
  color: var(--fg-tertiary);
}
``````

---

> 命令面板、侧边栏、Console、Settings 等面板视觉规范见 07c-侧边栏与命令面板视觉规范.md。

## 2.20 SSE 面板视觉规范（请求编辑页内嵌）

> SSE 面板在请求编辑页的请求面板中作为独立 Tab 出现，与 Params/Headers/Body/Auth 并列。

### 2.20.1 SSE Tab 布局

```
请求 Tab 栏: [Params] [Headers] [Body] [Auth] [Scripts] [SSE] [Settings]
                                    ↑ 新增 SSE Tab（青色标签）
SSE Tab 内容:
┌──────────────────────────────────────────────────┐
│ URL: https://api.example.com/events   [Connect] │ 44px URL 栏
│ Headers: [KV Editor 2 行]                       │ 可折叠
│ ──────────────────────────────────────────────── │
│ Events (23)                                     │ 28px header
│ ┌──────────────────────────────────────────────┐ │
│ │ Event     │ Data          │ ID    │ Time     │ │
│ │ message   │ {"type":"up"} │ 42    │ 14:32:01 │ │
│ │ update    │ {"ts":12345}  │ 43    │ 14:32:15 │ │
│ │ message   │ {"type":"dn"} │ 44    │ 14:32:30 │ │
│ └──────────────────────────────────────────────┘ │
│                                                  │
│ ┌──────────────────────────────────────────────┐ │
│ │ Last-Event-ID: [42]  [Reconnect]             │ │ 28px footer
│ └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

### 2.20.2 SSE CSS 规范

```css
.sse-tab {
  height: 100%;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.sse-url-bar {
  height: 44px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 0 12px;
}

.sse-url-bar input {
  flex: 1;
  height: 32px;
  background: var(--color-bg-input);
  border: 1px solid var(--color-border-default);
  border-radius: var(--radius-md);
  padding: 0 12px;
  color: var(--color-fg-primary);
  font-family: var(--font-mono);
  font-size: 12px;
}

.sse-url-bar input:focus {
  border-color: var(--color-border-focus);
  box-shadow: 0 0 3px oklch(0.55 0.2 275 / 0.25);
}

.sse-connect-button {
  width: 40px;
  height: 32px;
  background: var(--color-method-sse, var(--color-accent-info));
  border-radius: var(--radius-md);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-sans);
  font-size: 11px;
  font-weight: 600;
}

.sse-connect-button.connected {
  background: var(--color-accent-danger);
}

.sse-event-header {
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 12px;
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 600;
  color: var(--color-fg-tertiary);
  text-transform: uppercase;
}

.sse-event-count {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 500;
  color: var(--color-brand);
  background: var(--color-brand-muted);
  padding: 2px 6px;
  border-radius: var(--radius-full);
}

.sse-event-table {
  width: 100%;
  border-collapse: collapse;
}

.sse-event-row {
  height: 32px;
  font-family: var(--font-sans);
  font-size: 11px;
  color: var(--color-fg-secondary);
}

.sse-event-row:hover {
  background: var(--color-bg-hover);
}

.sse-event-row .event-name {
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 600;
  color: var(--color-method-sse, var(--color-accent-info));
}

.sse-event-row .event-data {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--color-fg-primary);
}

.sse-footer {
  height: 28px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 0 12px;
}

.sse-footer input {
  height: 24px;
  width: 120px;
  background: var(--color-bg-input);
  border: 1px solid var(--color-border-default);
  border-radius: var(--radius-md);
  padding: 0 8px;
  font-family: var(--font-mono);
  font-size: 10px;
  color: var(--color-fg-primary);
}

.sse-reconnect-button {
  font-family: var(--font-sans);
  font-size: 10px;
  font-weight: 500;
  color: var(--color-brand);
  padding: 4px 8px;
  border: 1px solid var(--color-brand);
  border-radius: var(--radius-md);
}
```

### 2.20.3 SSE 状态矩阵

| 状态 | Connect 按钮 | 事件区 | Footer |
|------|-------------|--------|--------|
| 未连接 | "Connect" 青色 | 空状态 + "Click Connect to start listening" | Last-Event-ID 可编辑 |
| 已连接 | "Disconnect" 红色 | 事件实时流入 | 显示当前 Last-Event-ID |
| 连接错误 | "Connect" 青色 | 红色错误提示 | 显示错误详情 |

