# 集���级配���设计

## 1. 概述

### 1.1 设计背景

**问题**：
- 当��� Collection 只��� `name` ��� `description`，缺少公���配置���力
- 每个���求都需要重复���置相同���认证���请求头、变���
- 无法实现"一处���置，���处生���"的最佳实践

**目���**：
- 支持���合级别的���共配���（变量、认���、请求���、脚���）
- 支���文件夹级别的���置（可选���
- 实现���置继承和���先级机制
- 提供直观���编辑界���

### 1.2 参考标���

**Postman Collection v2.1**：
- Collection-level variables
- Collection-level auth
- Collection-level pre-request scripts
- Collection-level test scripts
- Folder-level inheritance

---

## 2. 数据模���设计

### 2.1 Collection 类型���展

**当前定���**：
```typescript
interface Collection {
  id: string;
  name: string;
  description?: string;
  items: CollectionItem[];
  createdAt: string;
  updatedAt: string;
}
```

**扩展���**：
```typescript
interface Collection {
  id: string;
  name: string;
  description?: string;
  items: CollectionItem[];
  createdAt: string;
  updatedAt: string;
  
  // ���增：���合级���置
  variables?: Variable[];           // 集合���量
  auth?: AuthConfig;                // 集合认���
  headers?: Header[];               // 公共请���头
  scripts?: {
    preRequest?: string;            // 前置���本
    postResponse?: string;          // 后���脚本
  };
}
```

### 2.2 Folder 类型���展

**当前定���**：
```typescript
type CollectionTreeNode =
  | { type: "request"; ... }
  | { type: "folder"; id: string; name: string; items: CollectionTreeNode[] };
```

**扩展���**：
```typescript
type CollectionTreeNode =
  | { type: "request"; ... }
  | { 
      type: "folder"; 
      id: string; 
      name: string; 
      items: CollectionTreeNode[];
      
      // 新���：文���夹级���置
      variables?: Variable[];
      auth?: AuthConfig;
      headers?: Header[];
      scripts?: {
        preRequest?: string;
        postResponse?: string;
      };
    };
```

### 2.3 配置���承模���

```typescript
interface ConfigInheritance {
  // ���置来���
  source: 'request' | 'folder' | 'collection' | 'environment' | 'global';
  
  // 是否覆���父级���置
  override: boolean;
  
  // 合并策略
  mergeStrategy: 'replace' | 'merge' | 'append';
}
```

---

## 3. 配置���先级���计

### 3.1 ���先级层���

```
请求���配置 (最高���先级)
    ↓
���件夹���配置
    ↓
集合级配���
    ↓
���境变量
    ↓
全局变量 (最低���先级)
```

### 3.2 不同配置项���合并策���

#### 3.2.1 变量（Variables）

**策���**：**���盖（Replace）**

```javascript
// ���例
全局变���:     { api_key: "global-key", timeout: "5000" }
环���变量:     { api_key: "env-key" }
集合���量:     { api_key: "collection-key", version: "v1" }
文件夹���量:   { version: "v2" }
请求变���:     ���

// 最���结果
{
  api_key: "collection-key",  // 集���级覆盖环���级
  timeout: "5000",            // 继���全局
  version: "v2"               // 文件���级覆���集合级
}
```

#### 3.2.2 认证���Auth���

**���略**：**替换（Replace）**

```javascript
// 示例
集合认证:   Bearer Token (token: "collection-token")
文件���认证: 无
请求���证:   API Key (key: "X-API-Key", value: "request-key")

// 最���结果：使���请求���认证
API Key (key: "X-API-Key", value: "request-key")
```

**特殊情况**：
- 如果���求设��� `auth: { type: "inherit" }`，则���用父级���证
- 如果请���设置 `auth: { type: "none" }`，则���使用任���认证

#### 3.2.3 ���求头（Headers）

**策略**：**合并（Merge）**

```javascript
// 示例
集合请���头:   { "Content-Type": "application/json", "X-API-Version": "v1" }
文件���请求���: { "X-API-Version": "v2", "X-Folder-ID": "123" }
���求请求���:   { "Authorization": "Bearer token", "X-API-Version": "v3" }

// 最终���果（同名 key 被���盖，不同��� key 合���）
{
  "Content-Type": "application/json",      // ���承集���
  "X-API-Version": "v3",                   // 请���级覆盖
  "X-Folder-ID": "123",                    // ���承文���夹
  "Authorization": "Bearer token"          // 请求级���增
}
```

#### 3.2.4 脚本���Scripts）

**策略**：**���联执���（Cascade）**

```javascript
// 执���顺序���Pre-request）
1. 集合级 Pre-request Script
2. ���件夹��� Pre-request Script
3. 请求级 Pre-request Script
4. 发送请求
5. 请求级 Post-response Script
6. 文件���级 Post-response Script
7. ���合级 Post-response Script
```

**���本上���文共���**：
- 所���脚本共享同一���上下���对象
- 上级���本设���的变���，下级脚���可以访问
- 下级脚本���以覆盖���级脚本设���的变���

---

## 4. UI 设���

### 4.1 集合���置入���

#### 4.1.1 侧边栏���键菜���

```
Collection: User API
���─ [右键���单]
│  ├─ Rename Collection
│  ├─ Edit Collection Settings  ← 新增
│  ├─ Duplicate Collection
│  ���─ Export Collection
���  └��� Delete Collection
```

#### 4.1.2 集合���题栏操���按钮

```
┌──���───���──���──���──���─────���──���───────���──���─┐
│ 📁 User API                    [⚙] │  ← 点击���轮图���打开设置
└─���────���──���────���──���──���────���───���──���────┘
```

### 4.2 集合���置弹窗

#### 4.2.1 弹窗���局

```
┌───���─────���──���──���──���──���────────���───���──���──────���──���────���──���┐
��� Collection Settings: User API                      [×] │
├─���────���──────���──���──���──���──────���───���──���────���───���──���────���──���
│                                                        │
│  ┌───���──���──���┬───���──���──���──���────���───���───���──���────���───���┐  │
│  ��� General  ���  Name                                │  │
���  │ Variables│  ┌──���──���──���───���──���────���─────���──���──┐  │  ���
│  │ Auth     │  ��� User API                       │  │  ���
│  │ Headers  │  ���──���──���──���──���─────���──���─────���──���──┘  │  │
���  │ Scripts  │                                      │  ���
│  └─���───���──���─┤  Description                         │  ���
│             │  ���────���──���───���──���────���──���───────���─┐  │  │
│             │  ��� User management API endpoints  │  │  │
│             │  │                                │  │  │
│             │  └─���────���──���──���──���──────���──���──���──���┘  │  ���
│             └──���─────���──���────���────���──���────���────���──���┘  ���
│                                                        │
���                              [Cancel]  [Save Changes] │
└─────���──���───���──���────���───���──���──���────���───���──���──���────���───���─┘
```

#### 4.2.2 ���签页���计

**General 标签**：
- Collection Name（必���）
- Description（���选）
- Created At / Updated At（只���）

**Variables 标���**：
```
┌─���──���────���──���────���───���──���──────���──���──────���──���───────���
│ Collection Variables                               │
├───���──���────���──���────���───���──���──────���──���────���───���──���───���
│ ���  Key              Value                     [×]  │
���──���──���────���───���──���─────���──���───────���──���───���──���────���──���
│ ☑  base_url         /api/users                [×]  ���
│ ☑  api_version      v1                        [×]  │
│ ☐  debug_mode       false                     [×]  │
│                                                    │
│ [+ Add Variable]                                   │
└─���──���──────���──���────���──���───���──���──���──���──────���──���──────���

���� Tip: Collection variables are available to all requests 
   in this collection. Use {{variable_name}} to reference.
```

**Auth ���签**���
```
┌─���───���──���────���───���──���────���───���──���────���──���──���──���──���──���
│ Collection Authorization                           │
├─���───���──���────���──���──���──���─────���──���────���───���──���───���──���─┤
│ Type: [Bearer Token ▼]                             ���
│                                                    │
│ Token                                              │
��� ┌─���──���──────���──���────���──���────���─────���──���────���──���───┐ │
│ │ {{auth_token}}                                 │ │
│ ���──���──────���──���────���──���──���────���──���──���────────���──���─┘ │
│                                                    │
│ Prefix                                             │
��� ┌���──���──���────���───���────���──���───���──���──────���──���────���──┐ │
│ │ Bearer                                         │ │
│ ���────���──���────���──���──���────���────���───���────���──���─────���─┘ │
│                                                    │
��� ☑ Apply to all requests in this collection        ���
└───���──���────���──���──���───���───���──���───���──���──────���──���────���─┘

💡 Tip: Requests can override this by setting their own auth.
```

**Headers 标���**：
```
┌────���──���───���──���───���──���────���───���──���─────���──���────���──���─┐
│ Collection Headers                                 │
├─���──���────���──���──���──────���──���────���──���────���──���────���────���┤
│ ☑  Key              Value                     [×]  │
├─���────���──���──���───���──���─────���──���──���──���────���──���────���──���─┤
│ ☑  Content-Type     application/json          [×]  ���
│ ☑  X-API-Version    {{api_version}}           [×]  │
│ ☐  X-Debug          true                      [×]  │
│                                                    │
│ [+ Add Header]                                     ���
└───���──���──���──���─────���──���──���───���──���────���──���──���──���──���──���┘

💡 Tip: These headers will be added to all requests.
   Request-level headers will override these.
```

**Scripts 标���**：
```
���──���──���────���──���─────���──���────���───���────���──���──────���──���──┐
│ Collection Scripts                                 │
├────���──���────���──���────���──���──���──���────���──���──���───���──���──���─┤
│ [Pre-request] [Post-response]                      │
���                                                    │
│ ┌───���──���──���────���──���──���──���──���──���──���───���─────���──���──┐ │
��� │ // This script runs before every request       │ │
│ │ // in this collection                          │ │
│ ���                                                │ ���
│ ��� const timestamp = Date.now();                 │ │
│ │ pm.collectionVariables.set("timestamp",       │ ���
│ │   timestamp);                                  │ ���
│ ���                                                ��� │
��� │ console.log("Collection pre-request script");  │ │
│ └─���──���─────���──���────���──���────���──���──���────���──���────���──��� │
│                                                    │
└─���────���──���──���──���────────���───���──���────���──���───���──���─────���

💡 Tip: Collection scripts run for every request.
   Execution order: Collection → Folder → Request
```

### 4.3 文件���设置（可���功能）

#### 4.3.1 文件夹右���菜单

```
Folder: Authentication
├─ [右键菜单]
│  ├─ Rename Folder
│  ├─ Edit Folder Settings  ��� 新增
│  ├─ Add Request
│  ���─ Add Subfolder
│  └─ Delete Folder
```

#### 4.3.2 ���件夹���置弹窗

**简化版**（只包���核心���置）：
- Variables
- Auth
- Headers

**不包含**：
- Scripts���可选，Phase 2 实现）

---

## 5. 配置���承可���化

### 5.1 请求详情���显示继���信息

#### 5.1.1 Headers 标���页

```
┌���────���────���──���────���───���──���───���──���──────���──────���──���──┐
│ Headers                                            │
├─���──���──���────���──���────���───���────���──���─────���──���────���──���──���
│ ☑  Content-Type     application/json    [Collection] │
│ ☑  X-API-Version    v2                  [Folder]     │
��� ☑  Authorization    Bearer xxx          [Request]    │
│                                                    │
��� [+ Add Header]                                     │
���─────���──���───���──���──���───���──���──���────���──���────────���──���───┘
```

**说���**：
- `[Collection]` ���签表���继承���集合
- `[Folder]` ���签表示继承���文件夹
- `[Request]` 标签表示���求级���置
- 继承���配置���显示为灰���，不���直接���辑
- 点击���跳转到���配置���置

#### 5.1.2 Variables 面板

```
┌─���──���────���───���──���────���────���────���───���──���────���──���──���──┐
│ Available Variables                                │
├─���────���──���────���──���──���────���──���────���──���────���──────���──���┤
│ ���� Collection Variables (User API)                 │
│    base_url = /api/users                           │
│    api_version = v1                                │
���                                                    │
��� 📂 Folder Variables (Authentication)               ���
│    auth_endpoint = /auth                           │
│                                                    │
│ 🌍 Environment Variables (Development)             │
│    host = localhost:3000                           │
│                                                    │
│ 🌐 Global Variables                                │
│    timestamp = {{$timestamp}}                      │
└─────���──���──���───���───���──���──���─────���──���──────���──���──���───���┘
```

### 5.2 配置冲���提示

当请求级配置覆盖���级配���时，显示提示���

```
┌──���──���──���────���──���────���───���──���────���──���──���────���──���──���─┐
│ ⚠️  Authorization Override                          │
├──���──���────���───���──���────���───���───���──���─────���──���─────���──���┤
��� This request uses its own authorization instead   │
��� of the collection-level Bearer Token.             │
│                                                    │
│ [Use Collection Auth]  [Keep Request Auth]        │
└─���──���──���────���──���──���──────���──���───���──���──────���──���──���──���┘
```

---

## 6. 实现计���

### 6.1 Phase 1���集合���配置���核心）

**数据���**：
1. ✅ 扩展 Collection 类型���义
2. ✅ 更新 collection-store.ts
3. ✅ 实现配置继���逻辑
4. ✅ 持久���到磁���

**UI 层**：
1. ✅ ���合设���弹窗（CollectionSettings.tsx）
2. ✅ Variables 标���页
3. ✅ Auth 标签页
4. ✅ Headers ���签页
5. ✅ Scripts 标���页
6. ✅ ���边栏右键菜���集成

**逻辑层**：
1. ✅ 请求发���时合���配置
2. ✅ 变量���析时���虑集���变量
3. ✅ 脚本���行时���联调���

### 6.2 Phase 2：文件夹级配���（增���）

1. ⭕ 扩展 Folder 类型���义
2. ⭕ ���件夹���置弹窗
3. ⭕ 文件夹���配置���承
4. ⭕ 配置可���化

### 6.3 Phase 3：���级功能���可选���

1. ⚪ 配置���板（���速创���常用配置���
2. ��� 配置���入/���出
3. ⚪ 配置���本历史
4. ⚪ 配置冲突���测和���决

---

## 7. 技术实���细节

### 7.1 配置合���算法

```typescript
function mergeConfigs(
  request: RequestConfig,
  folder: FolderConfig | null,
  collection: CollectionConfig
): ResolvedConfig {
  // 1. ���并变量（���先级���请求 > 文件��� > 集��� > 环境 > 全局���
  const variables = {
    ...globalVariables,
    ...environmentVariables,
    ...collection.variables,
    ...folder?.variables,
    ...request.variables,
  };

  // 2. ���定认证���优先级���请求 > ���件夹 > 集合���
  const auth = request.auth?.type === 'inherit'
    ? (folder?.auth || collection.auth)
    : (request.auth || folder?.auth || collection.auth);

  // 3. 合并���求头（同���覆盖���不同名���并）
  const headers = [
    ...collection.headers || [],
    ...folder?.headers || [],
    ...request.headers || [],
  ].reduce((acc, header) => {
    acc[header.key] = header.value;
    return acc;
  }, {});

  // 4. 收���脚本（按���行顺���）
  const preRequestScripts = [
    collection.scripts?.preRequest,
    folder?.scripts?.preRequest,
    request.scripts?.preRequest,
  ].filter(Boolean);

  const postResponseScripts = [
    request.scripts?.postResponse,
    folder?.scripts?.postResponse,
    collection.scripts?.postResponse,
  ].filter(Boolean);

  return {
    variables,
    auth,
    headers,
    preRequestScripts,
    postResponseScripts,
  };
}
```

### 7.2 脚本���行引擎

```typescript
async function executeScripts(
  scripts: string[],
  context: ScriptContext
): Promise<ScriptContext> {
  let currentContext = { ...context };

  for (const script of scripts) {
    try {
      const result = await executeScript({
        code: script,
        context: currentContext,
      });

      // 合并脚���修改���上下文
      currentContext = {
        ...currentContext,
        ...result.modifiedContext,
      };

      // 记录日志
      console.log(`Script executed: ${result.logs}`);
    } catch (error) {
      console.error(`Script error: ${error}`);
      // 继���执行���一个���本
    }
  }

  return currentContext;
}
```

### 7.3 持���化格���

**IPC Collection File 扩���**：
```typescript
interface IpcCollectionFile {
  id: string;
  name: string;
  description?: string;
  items: IpcCollectionItem[];
  created_at: string;
  updated_at: string;
  
  // 新增
  variables?: Array<{ key: string; value: string; enabled: boolean }>;
  auth?: IpcAuthConfig;
  headers?: Array<{ key: string; value: string; disabled: boolean }>;
  scripts?: {
    pre_request?: string;
    post_response?: string;
  };
}
```

---

## 8. 用户体���优化

### 8.1 智能提示

**变���自动补全**：
- 在���入框���输入 `{{` 时，���动显���可用变量���表
- 按作���域分组���示（Collection → Folder → Environment → Global）

**配���继承提示**：
- 鼠标���停在继承的���置项上，���示来���信息
- 提���"跳转到���配置"的快捷���作

### 8.2 批量���作

**批量���用/禁用**：
- 选中���个变量/请���头，���量启���或禁���
- ���捷键：Ctrl+E（���用）���Ctrl+D（���用）

**批量���入**：
- 从 JSON/CSV 导入���量
- ���其他集���复制���置

### 8.3 ���置预���

**实���预览**：
- 在编���集合配���时，���时显示影响���请求数���
- 显示配���生效���范围（"将应���到 15 个请���"）

---

## 9. 测试用例

### 9.1 ���置继承���试

```typescript
describe('Config Inheritance', () => {
  it('should merge headers correctly', () => {
    const collection = {
      headers: [{ key: 'Content-Type', value: 'application/json' }],
    };
    const folder = {
      headers: [{ key: 'X-Folder', value: 'test' }],
    };
    const request = {
      headers: [{ key: 'Authorization', value: 'Bearer token' }],
    };

    const merged = mergeConfigs(request, folder, collection);

    expect(merged.headers).toEqual({
      'Content-Type': 'application/json',
      'X-Folder': 'test',
      'Authorization': 'Bearer token',
    });
  });

  it('should override auth correctly', () => {
    const collection = { auth: { type: 'bearer', token: 'col-token' } };
    const request = { auth: { type: 'apikey', key: 'X-API-Key' } };

    const merged = mergeConfigs(request, null, collection);

    expect(merged.auth.type).toBe('apikey');
  });
});
```

### 9.2 脚本执行测���

```typescript
describe('Script Execution', () => {
  it('should execute scripts in order', async () => {
    const scripts = [
      'pm.collectionVariables.set("step", "1");',
      'pm.collectionVariables.set("step", "2");',
      'pm.collectionVariables.set("step", "3");',
    ];

    const context = await executeScripts(scripts, {});

    expect(context.collectionVariables.get('step')).toBe('3');
  });
});
```

---

## 10. 迁移策���

### 10.1 现���数据兼容

**向后兼容**：
- 现有 Collection ���件不���含新字段���默认���空
- 加���时自动补充���认值
- 不���响现���功能

**迁移���本**：
```typescript
function migrateCollection(oldCollection: OldCollection): NewCollection {
  return {
    ...oldCollection,
    variables: [],
    auth: undefined,
    headers: [],
    scripts: undefined,
  };
}
```

### 10.2 Postman ���入支���

**导��� Postman Collection v2.1**：
- 自动识别 collection-level auth
- 自动���别 collection-level variables
- 自动识别 collection-level scripts
- 保持配���层级���系

---

## 11. 文档���帮助

### 11.1 内置文档

**帮助���钮**：
- 每个���置标���页右上角���加 `[?]` 帮助���钮
- 点击���示该配置项���说明���示例

**示例模���**：
- 提供常用���置模板���REST API、GraphQL、OAuth 2.0）
- 一键应用���板

### 11.2 用户指南

**文���章节**：
1. 集合级配���概述
2. 变���作用���和优先级
3. 认证继���机制
4. 脚本编写指���
5. 最���实践

---

## 12. 总结

### 12.1 核心价���

1. **���少重复���置**���一次配置���多处生���
2. **提���一致���**：统一���认证���请求头
3. **增强���维护���**：集中管理���共配���
4. **对标 Postman**：功���完整性达���行业标���

### 12.2 ���现优先级

**必须实���**（Phase 1）：
- ✅ 集合��� Variables
- ✅ 集合级 Auth
- ✅ 集���级 Headers
- ✅ 集合��� Scripts
- ✅ 配置继���逻辑

**重���功能**（Phase 2���：
- ⭕ ���件夹级配置
- ��� 配置���视化
- ⭕ 智���提示

**可���功能**（Phase 3）：
- ⚪ 配置模板
- ��� 批���操作
- ⚪ ���本历史

### 12.3 与环���变量设计���关系

**协同工���**：
- 环���变量：跨���合的全���配置（如 host、api_key）
- 集合变���：集合���部的���部配置（��� base_path���version）
- ���者通过变���解析器统一���理，���先级清晰

**用���心智模型**：
```
全局���量 (Global)
    ↓
���境变量 (Environment: Dev/Staging/Prod)
    ↓
集合���量 (Collection: User API)
    ���
文件夹变量 (Folder: Authentication)
    ↓
���求变��� (Request: Login)
```
